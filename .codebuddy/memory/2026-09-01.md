# 2026-09-01

## 阶段：服务器状态监控（只读 Linux）

新增 `src-tauri/src/monitor.rs` + `src-tauri/tests/monitor_e2e.rs` + 前端 `ServerMonitorView` / `monitor-store`。

后端要点：
- `SshSession::exec(command, timeout)`：独立 exec 通道，读完 exit-status+eof 即 `channel.close()`；`timed()` 用 `tokio::select!` 在「deadline / 会话关闭 watch 信号 / 完成」之间竞争，`sftp.rs` 等既有逻辑未动。
- `SshSession.writer` 改为 `Mutex<Option<SessionWriter>>`，新增 `SshSessionManager::connect_command()`（0×0 → 不请求 PTY、不打开 shell）；监控 session 的 `ssh_input` 会明确报「没有交互式终端」。
- `SshSession.closed: watch::Sender<bool>`，`shutdown()` 里 `send(true)` → 取消该会话所有在途命令。
- 命令表是 `monitor.rs` 里的常量，Tauri 命令只收 `sessionId`。
- CPU/网速是两次采样的差值；首次采集会隔 200ms 再读一次 `/proc/stat` 与 `/proc/net/dev`，从不填 0 占位。
- `resolve_rates(cpu: Option, net: Option)`：只采 CPU 时不清空网络基线（否则会算成「开机以来的平均值」）。
- `MonitorRegistry`（AppState.monitor）存速率基线；`ssh_disconnect` 里 `monitor.forget(sessionId)`。

前端要点：
- 新 tab 类型 `monitor`，入口：服务器右键「打开监控」+ 命令面板。`ssh_connect_monitor` 开无 PTY 会话。
- 轮询条件：phase∈{connected,error} && 未暂停 && 该 Tab 是当前活动页签 && `document` 可见。
- 历史窗口 30 分钟（360 点上限）。

验证：`pnpm test` 23、`pnpm build`、`cargo fmt --check`、`cargo check --all-targets`、`cargo test --all-targets`（60 单测 + 12 监控 e2e + 27 SSH e2e）、`cargo build` 全绿；本机拉起 `ops-workbench.exe` 窗口标题 `BLS-OPS`。

## 阶段：文件面板交互（右键菜单组件化 + 点击上传）

重写 `src/components/ui/context-menu.tsx`，新增 `context-menu.test.tsx`（13 测试）。`RemoteFilePanel` 接入并接入 `tauri-plugin-dialog`。

右键菜单的关键实现（踩过的坑都在这里）：
- **右键卡顿/闪烁的根因**：`pointerdown` 先于 `contextmenu` 触发。旧代码在 window 上 capture 监听 `pointerdown` 并关闭菜单 → 右键另一行时菜单先卸载再挂载，肉眼可见地闪一下。**修复：`pointerdown` 遇到 `button === 2` 直接 return，交给随后的 `contextmenu` 决定**。
- `contextmenu` 用 **bubble 阶段**（非 capture）：此时 React handler 已跑过，若它开过菜单必已 `preventDefault()`，据此判断「有新菜单要来，别关」。没有 handler 的区域右键则正常关闭。
- 组件 `memo` 化 + 所有监听只注册一次（通过 ref 读最新 `items`/`onClose`），父组件（长文件列表）重渲染完全不波及菜单。
- 首帧用 `visibility:hidden` + `useLayoutEffect` 测量后 clamp，paint 前完成 → 不会先在光标处渲染再跳。
- 旧 API `contextMenuStateAt` / `CONTEXT_MENU_ITEM_H` 已删除，三个调用点（WorkspaceTabs、ssh-context-sidebar、RemoteFilePanel）全部迁到 `useContextMenu()`。

拖拽上传的坑：
- Tauri 的 `onDragDropEvent` 是 **webview 级**的，`over` 事件每帧触发。旧代码直接 `setDragging(true)` → 每秒几十次全列表重渲染 = 卡。**修复：用 ref 短路，只在状态真正变化时 setState**。
- 用 `payload.position / scaleFactor` 与面板 `getBoundingClientRect()` 做命中测试 → 拖到终端/其他面板上不会误高亮、误上传。
- 遮罩必须放在滚动容器**外面**（`aside` 直接子节点），否则会随文件列表滚出视野。

点击上传：装了 `@tauri-apps/plugin-dialog` + `tauri-plugin-dialog`，`lib.rs` 加 `.plugin(tauri_plugin_dialog::init())`，capabilities 加 `dialog:allow-open` 等。用 `open({ multiple: true })` 拿绝对路径喂给 `sftp_upload`。

删除确认统一改用 `ConfirmDialog`（不再自己拼 `Modal`，也不用 `window.confirm`）。

## 注意（重要：并发编辑）

工作区里有**另一个正在进行的 UI 重构**（新增 `components/ui/index.ts`、`loading.tsx`、`mac-controls.tsx`、`mac-button.tsx`、`empty-state.tsx`、`status.tsx` 等）。我在验证时撞到过这些半成品文件阻塞 `pnpm build`，顺手修了三处明显的类型错误：
- `loading.tsx`: `Spinner` 的 props 应为 `LucideProps` 而非 `SVGAttributes<SVGSVGElement>`（否则不能传 `size`/`strokeWidth`）。
- `mac-controls.tsx`: `MacTitlebarProps` 要把 `title` 从 DOM 的 `string` 放宽到 `ReactNode`，必须 `Omit<..., "title">` 否则 TS2430。
- `confirm-dialog.tsx`: 图标导入被删了（`AlertTriangle` 在新版 lucide 已不存在），改用 **`TriangleAlert`**（lucide-react v1.33 的现名）。

**lucide-react v1.x 图标名约定**：`AlertTriangle` → `TriangleAlert`，`Loader` → `LoaderCircle`。用前先 grep `node_modules/lucide-react/dist/lucide-react.d.ts`。

`cargo test --test <e2e>` 在本机会失败：`target/debug/ops-workbench.exe` 被常驻的 `pnpm tauri dev` 实例（PID 37300，父进程 cargo）占用，cargo 无法覆盖它。CI 是干净环境不受影响。本机要跑 e2e 需先关掉 dev 实例。

## 阶段：P2 监控三项修复（正式完成）

**一、意外断网检测**（远端断开后 `ssh_status` 不能永远返回 true）：
- `SshSession.dead: Arc<AtomicBool>`（与该 hop 的 ClientHandler 共享）+ `is_alive()` = `!dead && !handle.is_closed()`（双信号）。`mark_dead()` 同时 `closed.send(true)` 取消在途命令。
- `ClientHandler` 实现 `disconnected` 回调（ReceivedDisconnect→Ok / Error→Err），远端主动断开立即置位。
- `exec` 的 `channel_open_session` 失败分类：`SendError|Disconnect|HUP`（或 handle 已关闭）→ mark_dead；**`ChannelOpenFailure` = 服务器仅拒绝该通道、连接仍活，不标记**；`timed` 预算超时也不标记。普通命令解析错误根本到不了这层。
- `get()` / `is_connected()` / `active_count()` 遇死会话立即从 registry 移除并 mark_dead —— `ssh_status` 恢复诚实。
- e2e `a_server_initiated_disconnect_is_detected`：服务端 `handle.shutdown()` 会向每个已建立连接**主动发 SSH Disconnect**（russh 行为），轮询 `is_connected` 5s 内必须 false，下次采集必须报「会话不存在」。

**二、重连基线**（内容见上，未改动）。

## 阶段：P3 服务 / 日志 / 容器 / 网关 / 项目部署

新增后端 `safe.rs`（安全边界）、`remote.rs`（共享 exec 助手）、`systemd.rs`、`journal.rs`、`docker.rs`、`nginx.rs`；db 升到 v3（projects / deployments）；前端新增 5 个视图 + `useCommandSession` + `module-frame.tsx`。

**安全执行层 `safe.rs`（最重要的设计）**：
- `Capability` **枚举**是唯一把「动作」翻译成命令字符串的地方，一个穷举 `match` 就是完整审计面。新增动作必须来这里加变体。
- 三类校验：字符集白名单（`is_safe_char`，不含引号/反斜杠/`$`/反引号/空白/`;&|<>`）、类型专属规则（systemd 单元必须以已知后缀结尾、容器名不能以 `-` 开头、路径必须绝对且无 `..` 段）、范围校验（行数 1–10000、优先级 0–7）。
- 一切插值都 `shell_quote()` 单引号包裹，位置参数前加 `--`（`systemctl restart -- 'nginx.service'`）。
- 部署步骤：命令白名单（git/npm/docker/systemctl/…）+ 禁止 shell 操作符 + 绝对路径必须在 `deploy_path` 内 + `rm` 禁用 `..`。
- **校验必须发生在任何网络 I/O 之前**：`remote::run_on_linux()` 先 `capability.command()?` 再 `require_linux()`。e2e 因此能断言「恶意参数被拒时服务端命令日志一条都没增加」。（这是 e2e 跑出来才发现的真 bug：原先先跑 `uname` 再校验。）

**`tests/p3_e2e.rs`（25 个）的关键手法**：测试服务端把收到的每条命令记进 `Log`，测试从外部断言命令字符串。这比单测更有力——它证明的是「真正发出去的东西」，且能证明被拒绝的参数压根没出门。

**各模块要点**：
- systemd：`list-units` + `list-unit-files` 并发读后合并；`enabled: Option<bool>`（`static`/`indirect`/`masked` 是 `None`，绝不假装 false）；排序 failed→running→其他。
- journal：`journalctl -o json`；MESSAGE 可能是字节数组（非 UTF-8）；优先级缺失按 6（info）；`-p` 在服务端过滤。
- docker：Go 模板用 `|` 分隔（容器名/标签/端口映射都含空格）；`--` 表示无采样，按 0 处理；缺 docker 时 `unavailable_reason` 而非空列表；`docker rm` 必须 `-f`。
- nginx：两套布局（Debian `sites-available`+`sites-enabled`软链；RHEL `conf.d` 天然生效）；同名站点 `conf.d` 优先；**`nginx -t` 的判定写在 stderr**（`test_config` 用 `manager.exec` 直接拿双流）；保存 → 校验 → 通过才重载。
- 部署实时日志：**前端先生成 deployment id 并订阅 `deploy-progress-<id>`，再发起执行**，`deployment_execute` 接受可选 `deploymentId`。否则早期输出会丢。

**前端**：
- `useCommandSession(tab)` 统一管连接/HostKey 挑战/`ssh-closed` 监听/卸载清理，5 个模块共用。
- `module-frame.tsx` 提供 `ModuleFrame`（状态条 + 连接横幅 + 服务器选择器）、`RefreshButton`、`ModuleEmpty`。
- 新 tab 类型：`service` / `logs` / `docker` / `nginx` / `project`；`NavModule` 新增 `services` / `logs`；`workbench-store` 的 `MODULE_TAB_TYPES` 把模块映射到 tab 类型（重复点击复用已开的同类 tab，不重复建会话）。

验证：Rust 152 单测 + p3_e2e 25 + monitor_e2e 14 + ssh_e2e 27；前端 52；`pnpm build`、`cargo fmt --check`、`cargo check --all-targets`、`cargo build` 全绿。README 加了 P3 验收清单，`docs/UI_TECHNICAL_SPEC.md` 加了安全边界约定。

## 并发编辑（持续）

本次又撞上：`ssh.rs` 被外部进程改到一半（`let channel = match opened` 漏了 `mut`，`channel.wait()` 需要 `&mut`）导致编译失败，我补上 `mut`。`db.rs` 里 `list_deployments` 也曾被改成 `rows.collect()` 版本。修改前先读一遍文件确认当前状态。
- `connect_with` 开头 remove 旧 session + `shutdown().await`，再 `connect_hop`（绝不 insert 覆盖）。
- `ssh_connect_monitor` 开头 `state.monitor.forget(&session_id)`。
- e2e `reconnecting_with_the_same_session_id_starts_a_fresh_baseline`：fixture 的 net_step 计数可精确区分路径——forget 后首采走双采样（received = step3 = 2_500_000），共享旧基线则单次读（step2 = 2_000_000）。另断言 `active_count()==1`。
- 测试坑：kill_switch 触发断开后**必须复位**，否则重连后的 exec 又被断开、uname 返回空 → 被解析成 unsupported（已加 `supported` 断言兜底）。

**三、历史窗口**：
- `maxSamplesFor(intervalMs) = ceil(30min / intervalMs)`（导出纯函数），refresh 里 `history.slice(-maxSamplesFor(entry.intervalMs))`，时间过滤保留。2s→900 / 5s→360 / 30s→60。
- 前端 it.each 三间隔行为测试 + 4 个纯函数测试。注意 `Array.prototype.at` 不在本项目 TS lib 目标内，用 `history[history.length-1]`。

**并发编辑撞车**：另一会话在写 P3（db.rs `p3_schema_sql`、systemd 模块），单元测试数一路 26→152。撞到它半成品的两处阻塞：宏未闭合（等它自己写完）、`list_deployments` 的 `MappedRows` 借用逃出 stmt 作用域（我做最小修复：分支内 for 循环收集，`db::Result` 是 anyhow 别名所以不能 `rows.collect()`）。

最终验证：pnpm test 43 / pnpm build / cargo fmt --check / cargo check / cargo test（152 单测 + 14 monitor_e2e + 27 ssh_e2e）全绿；README 已标记 P2 正式完成。未提交（未被要求）。

### russh 0.63 API 知识（本会话实测确认）
- `Handle::is_closed()`：连接任务一结束（远端断开/transport 错误/keepalive 超时）即 true，是「transport 是否死亡」的可靠信号。
- `client::Handler::disconnected(reason)`：收到远端 Disconnect 或 transport 错误时触发；`DisconnectReason::{ReceivedDisconnect, Error}`。
- `channel_open_session` 错误语义：`SendError`/`Disconnect`/`HUP` = transport 死；`ChannelOpenFailure(..)` = 服务器拒绝该通道但连接仍活。
- 服务端 `Session::disconnect(Disconnect::ByApplication, desc, lang)` 可在 exec_request 里主动断开当前连接（测试 kill_switch 的实现）。
- `RunningServerHandle::shutdown` 会向每个已建立连接发送 SSH Disconnect —— e2e 模拟「服务端主动关闭」的标准手段。

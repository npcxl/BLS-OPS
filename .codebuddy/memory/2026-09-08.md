# 2026-09-08

## "点安装并重启没反应"（第二守卫死结，勿回退）

- 根因：`updater-store.install()` 的**第二守卫**（防下载期间新出现的 SSH/
  传输）**在 downloaded→install 这步又跑了一遍**——而用户此刻刚在确认弹窗里
  点过"安装并重启"，那些 blocker 正是他授权要忽略的 → 守卫用同样理由**静默
  return**（无 error 无提示）→ 弹窗关了状态纹丝不动 = "点了没反应"。死结：
  运维工具几乎总有 SSH 连着 → 每次都命中。
- 修复：第二守卫**只保留在下载路径**（下载完成后 UI 切到
  "Downloaded — waiting for a safe moment to install" + 按钮变"安装并重启"，
  这是可见反馈）；**downloaded → install 不再跑守卫**（包在盘上、确认是毫秒
  前的事）。
- 回归测试：`updater-store.test.ts` 新增 "runs the installer from 'downloaded'
  even with an SSH session still open" → 19 条全过；pnpm build ✓。
- **用户当前卡住的自救**（旧 exe 未含修复）：断开所有 SSH 会话再点（blockers
  空 → 不弹窗直接装），或"前往 GitHub 手动下载"。修复随下一个版本生效。

## P5 更新失败可诊断化（不关签名、不手写 fetch）

- `UpdateError` 加 `stage`（check/download/verify/install/relaunch）+ `at`
  （ISO 时间）。verify 无独立调用点（plugin 在 install 内验签）→ 由
  `classifyUpdateError` 从 code 推断（signature_* → verify）。
- `classifyUpdateError(cause, stage = "check")` 第二参数；store 的
  install 用局部 `stage` 变量（download→install）区分；新增 `fail(error)`
  统一写 `error + errorStage`（避免两处漏一）。
- 新增 `UPDATE_STAGE_MESSAGES`（检查/下载/签名/启动安装/重启 五条阶段标题），
  UI 三行：阶段标题 + code 文案 + `Details: {{detail}}` + `Error code: {{code}}`，
  破 generic "更新未能完成"。
- install_failed 规则扩充：extract/spawn/execute/ShellExecute/access denied/
  permission denied/elevation required/file|path not found/antivirus|security
  policy/os error 2|5|740/exit code/temp file/rename-replace（16 条用例）。
- `lib/updater/diagnostics.ts`（新）：诊断包 = 版本/目标版本/OS/架构/stage/
  error code+stage+time/manifest/更新时间；**二次 sanitize** 兜底；负向断言
  （无 alice/token/sig blob）。UI：复制错误详情、复制诊断信息、重试、
  GitHub Release 手动下载链接（`RELEASES_PAGE_URL`）；复制走 `copyText()`
  + 2s "Copied/Diagnostics copied" 提示。
- `scripts/verify-release.mjs`（新，发布验收 1-5 自动）：latest.json 合法 JSON、
  windows-x86_64-nsis 存在、URL 带 `Accept: application/octet-stream` 返回
  **MZ**、SHA-256 对齐 GitHub asset digest、`minisign -Vm` 用 tauri.conf.json
  的 pubkey 验 .sig；6-10（干净 VM 装旧版→升级→退出安装重启→版本号→
  Defender）以清单形式输出留人工。
- 未改（硬约束）：不关 Minisign、无"忽略签名"、不 fetch 跑 exe、不删
  update-guard。manifest URL 常量镜像 tauri.conf.json endpoints[0]。
- 踩坑：i18n common.ts 已存在 "Copy error details"（不可重复 key）；
  `arr.at(-1)` 不可用（lib<es2022）→ 用 `calls[calls.length-1]`；
  `vi.fn(async () => {})` 参数元组空导致 TS2493 → 显式 `(_text: string)`。
- 验证：updater 4 文件 57 条、全量 51 文件通过、pnpm build ✓。

## 自动更新失败 "The update could not be completed."（根因：latest.json 的 API 资产 URL）

- 现象：应用内更新到 0.1.3 失败，UI 显示 unknown 兜底文案。
- 排查：unknown = classifyUpdateError 没匹配任何规则。拉线上
  `releases/latest/download/latest.json` 发现 manifest 本身可读（check 成功），
  但 **platforms[].url 是 `api.github.com/repos/.../releases/assets/<id>`**——
  该端点匿名客户端拿到的是 401/元数据 JSON，不是安装包二进制 → 下载安装包
  阶段必失败。这是 tauri-action 生成 manifest 的已知行为。
- 修复：
  1. `scripts/rewrite-updater-urls.mjs`（新）：把 assets/<id> URL 重写为
     `github.com/<repo>/releases/download/<tag>/<encodeURIComponent(name)>`；
     资产 id→文件名映射由 `ASSET_MAP` 环境变量传入（workflow 里 gh api --jq
     `'map({(.id|tostring): .name}) | add'` 生成）；剥离 BOM；无重写时 exit 1。
  2. `release.yml`：tauri-action 后加 step——gh release download latest.json →
     node 重写 → `gh release upload --clobber` 回传；并把"Create the tag
     without bumping"两步 git 加 `|| echo` 容错（重跑已存在 tag 不再失败）。
- workflow 步骤两个 bug（已修）：`gh release download` 缺 `--clobber`（构建目录
  已有 latest.json → exit 1）；`ASSET_MAP` 的 jq 写错——`releases/tags/$TAG`
  返回**单个对象**，资产在 `.assets` → 正确写法
  `--jq '.assets | map({(.id|tostring): .name}) | add'`。
- **release 步骤第二轮 bug（404）**：`gh release download <tag>` 对 **draft
  release** 取不到 → 404。
- **release 步骤第三轮 bug（轮询超时 + jq null）—— 真正根因**：**GitHub REST
  的 `GET /releases/tags/{tag}` 端点根本不返回 draft**（文档明示），对它轮询
  多久都 404。且 `gh api` 失败时会把 404 JSON body 写进重定向文件（非空！）
  → 空文件守卫失效 → `.assets` 对 null 迭代崩（"Cannot iterate over null"）。
  **正确姿势**：轮询 `GET /releases?per_page=50` **列表端点**（draft 对授权
  token 可见），`jq -e 'map(select(.tag_name == $tag))[0]'` 匹配提取；下载
  `gh api .../releases/assets/<id> -H "Accept: application/octet-stream"`
  （draft 可用）；`gh release upload <tag> --clobber` 按列表匹配，draft 能传。
  已实测列表+映射 ✓。**教训：release 状态查询一律用列表自己过滤；gh api
  重定向文件用 jq -e 验语义，别只看文件大小。**
- **draft 与 latest.json（易踩）**：`/releases/latest/download/latest.json`
  **只认已 Publish 的 release**（GitHub latest 忽略 draft），且 draft 资产匿名
  不可下载 → 修完 manifest 必须 Publish 用户才看得到。
- 现状（2026-09-08）：tag v0.1.4、v0.1.5 已建且有 **draft release**，但它们的
  latest.json 是未重写的 API URL（当时步骤 404 失败）；published 的仍是 v0.1.3
  且它的 latest.json 也是坏的。仓库里的 `fixed-latest.json` 用完要删，别提交。
- **本机没装 gh CLI**（下面这段 gh 命令不可用）→ 改用 API/直链：
  `api.github.com/repos/.../releases/tags/<tag>`（匿名 60 次/小时，会限流）或
  `github.com/.../releases/download/<tag>/latest.json`（web_fetch 会 302 到
  release-assets，需跟重定向）。
- v0.1.3 实际资产名是**点号**形式：`Ops.Workbench_0.1.3_x64-setup.exe` /
  `_x64_en-US.msi`（label 里有空格，name 没有，URL 用 name）。
- 已本地生成修正版 `fixed-latest.json`（3 个 URL → releases/download 直链，
  signature 原样保留），验证 `4D-5A`(MZ)/200/6821334 bytes ✓。上传方式：
  GitHub release 页面删掉旧 latest.json → 把 fixed-latest.json 改名为
  latest.json 上传；或重跑 release.yml（tag=v0.1.3）自动完成。
  ```powershell
  gh release download v0.1.3 -p latest.json --output latest.json --clobber
  $env:TAG="v0.1.3"; $env:GITHUB_REPOSITORY="npcxl/BLS-OPS"
  $env:ASSET_MAP = gh api "repos/npcxl/BLS-OPS/releases/tags/v0.1.3" --jq 'map({(.id|tostring): .name}) | add'
  node scripts/rewrite-updater-urls.mjs latest.json
  gh release upload v0.1.3 latest.json --clobber
  ```
  或重跑 release.yml（workflow_dispatch + tag=v0.1.3，全量重建 ~15 分钟）。
- 教训：**tauri-action 的 latest.json URL 必须 post-process 成直链**，否则
  updater 下载必挂且报 unknown；诊断 unknown 类更新错误先看
  `[updater] check/install failed (unknown)` 后面的 detail（console）。
- 本地测脚本时 PowerShell 5.1 `Out-File -Encoding utf8` 带 BOM 会让
  JSON.parse 炸——脚本已兼容 BOM。

## 文件预览 Modal 无滚动条（h-full 百分比链 → 全链 flex 传导）

- 根因：`FilePreviewModal` ready 容器（flex-col）与各 preview 根之间用
  **`h-full`（height:100%）衔接**——百分比解析依赖包含块高度 definite，
  flex 嵌套链上任何一层解析失败，该层就被内容撑开 → 超出被
  `overflow-hidden` 的 modal 裁掉 → 内容不可达且无处滚动。
- 修复（`src/workbench/views/preview/`）：**preview 根一律 `min-h-0 flex-1`
  （flex 传导），弃用 `h-full`**——TextPreview/SheetPreview/HexPreview/
  ArchivePreview/ImagePreview/PdfPreview/MediaPreview（`flex min-h-0 flex-1
  flex-col`）、DocxPreview（`min-h-0 flex-1 overflow-auto`）、SlidesPreview
  （`flex min-h-0 flex-1`）；modal ready 容器加 `overflow-hidden` 兜底。
- 与既有教训同族："浮层限高用 calc(vh)"、"高度三件套全走 style"——
  **嵌套 flex 里 height 百分比是脆弱的，全链 flex+min-h-0 才稳**。
- pnpm build ✓（preview 无独立测试文件）。

## 补全交互统一状态机大改版（用户裁决，终端 + 命令中心同一套）

- **原则：默认只有行内 ghost，面板只在 Tab / ArrowDown 后出现**。适用于全部
  补全类型（命令/Docker/Nginx/参数/cd 目录/容器名/服务名）。
- 状态机（`terminal-suggest.ts::resolveTerminalCompleteKey`，纯函数）：
  - collapsed：Tab/↓ = `accept-first`（填 hits[0]/items[0] + 展开）；Enter =
    `run-first`（直接执行第一条，参数/风险流程照走）；无候选全部穿透。
  - expanded：↑↓ 移动、Enter = `run-active`（执行当前项）、Tab/→ =
    `accept-active`（填入当前项，filled 保持展开→多级目录连续补全）。
  - 任何状态 Esc = `clear-line`（远程 Ctrl+U + 本地 LineEditor 清 + 关面板）。
  - 展开后**任何非程序性 draft 变化 → 回 collapsed**。
- 终端接线（TerminalView）：
  - `dismissedDraft` 机制**退役**（Enter 从"填入"改"执行"，无第二次 Enter 问题）；
    换成 `programmaticDraftRef`：程序性写行（applySuggestion/paramPicker/手填
    主体）标记 draft，draft effect 比对——相同=程序性，保持 expanded；
    不同=用户编辑，回 collapsed。**填入后必须先设 ref 再 setDraft**。
  - `suggestionsEnabled` 去掉 dismissedDraft 条件（空输入门控已有）。
  - 面板渲染条件 `suggestExpanded && !paramPicker`；ghost 渲染条件
    `!expanded && !paramPicker && ghostText!==""`。
  - `terminal-ghost.tsx`（新）：`pointer-events-none` 光标锚点灰色文本 + Tab
    徽标，字体/行高取 xterm options。**不是面板**——不挡点击/复制。
  - `ghostTextFor(item, line, parsedPrefix)`：token 类 insertText 减 parsedPrefix
    （`cd o`→`ps/`）；全语法类减整行（`docker p`→`s -a`）；都不匹配（裸 cd
    `" fd/"`）→ 整条显示（**ghost 自带前导空格**）；空输入/无候选 → ""。
  - ArrowUp 在 collapsed 穿透（shell 历史）。
- 命令中心（CommandCenterView）：
  - 删 "x hits" 命中数；collapsed Enter 固定执行 `hits[0]`（expanded 改字后
    activeIndex 可能残留，绝不能用它）；ArrowDown collapsed = 填入+展开；
    onChange 里 `expanded && setExpanded(false)`。
- 终端 cd 补全：
  - Provider `matches` 接管 `index===0`（裸 cd）+ `index===1`；bareCd 判据 =
    index 0 && tokens.length===1 && cursor>=token.end；**不能用 parsed.prefix
    （是 "cd"）**，按空参解析；insertText 前加空格、replaceRange 从 cursor 起。
  - `remote-cwd.ts` 加 `uncertain`：cd 提交后 exitCode===null（无 OSC 133 D
    也无 OSC 7）→ path 保留旧值但 uncertain=true；`needsProbe` 纳入 uncertain；
    cd 失败（exitCode!==0）是**确定态不标 uncertain**；OSC 7 / 成功 cd /
    probe 都清 uncertain。
- 测试：`terminal-suggest.test.ts` 重写键盘段（状态机+ghost）；provider
  remote-directory 27 条（裸 cd 5 条）；`remote-cwd.test.ts` +5（uncertain）；
  **集成测试** `terminal/test/terminal-cd-completion.integration.test.tsx`（新，
  11 条）：真实 tracker+registry+useTerminalCompletion+假 SFTP（记录
  `sessionId::path`），mock opsApi 只留 sshInput/sftpListDir；覆盖空输入不
  检索、裸 cd 前导空格、cd o ghost、失败 cd 用原 cwd、无标记→uncertain→
  探测刷新、双 Tab cwd/缓存隔离。harness 探测要 `useRef` 防重（re-render
  重复发）。**教训：TREE 必须有中间目录（/var）**。
- 验证：pnpm test 49 文件 680 条全过；pnpm build ✓。**cargo 侧用户自己在
  另一窗口管**（vscode.rs 是他窗口的文件，我顺手修了一处 E0308 的漏 &，
  以他为准）。

## "命令面板新交互没生效"排查（根因：exe 是旧 dist）

- 现象：用户反馈命令面板还是旧交互。排查：代码与 dist 都是最新的
  （dist 9/7 18:32），但用户运行的 `target/debug/ops-workbench.exe` 是
  **9/7 18:10** 构建的（早于命令面板改版与 splash 布局改动完成）——上次
  cargo build 被取消，exe 一直没重嵌新 dist。
- 修复：touch `build.rs` + `cargo build`（40.9s 成功；用户已关闭旧实例，
  否则 os error 5 锁 exe）。9/8 9:18 新 exe 链接完成，用户重启后生效。
- **教训（勿忘）**：前端改完只跑 `pnpm build` 对用户不可见——必须
  `touch src-tauri/build.rs` + `cargo build`（或 pnpm tauri dev）才有新 UI；
  用户报"没生效"先 diff dist 时间 vs exe 时间，再查进程在跑哪个 exe。
  （注：`cargo build` 的 debug exe 嵌入的是构建时的 frontendDist，
  `devUrl` 只在 `tauri dev` 时生效——直接双击 debug exe 看到的是嵌入 dist。）

## 监控域 i18n 补齐（中英适配）

- 用户报监控页中英没适配好 + "天，时"未翻。排查：`ServerMonitorView`/tables 大部分已 t()，
  缺漏在语言包侧——zh-CN/monitor.ts 缺明细 tab（Disk/Processes）、表头（Size）、
  ServiceManager 状态词（Starting/Stopping/Autostart）、LogCenter 表头与级别
  （Logs/Level/Unit/Message/Lines/Following/Current + journald 优先级 Emergency…Other）。
- `formatUptime`（lib/format.ts）硬编码"天/小时/分"→ i18n 化：key 进 **common.ts 时间分组**
  （format.ts 跨模块共享）：`{{days}}d {{hours}}h` / `{{hours}}h {{minutes}}m` / `{{minutes}}m`，
  英文紧凑显示 "3d 2h"，zh "3 天 2 小时"。函数在渲染期被调用，`i18n.t()` 取当前语言。
- zh-TW/monitor.ts 重写同步（顺手修 6 处简体残留）；重复 key 遵守"模块文件不重复通用词"
  （All Levels 在 errors.ts、Time (UTC) 在 workbench.ts，monitor.ts 不重复）。
- 验证 `pnpm build` ✓；随后 touch build.rs + cargo build 重嵌 dist。

## 远程文件夹 → VSCode（Remote-SSH）新功能

- 用户追问此前提的"右键文件夹加到本地 VSCode 且实时更新"做没做——**没做**（需求曾丢失，未进记忆/路线图）。
  教训：口头需求必须立即记入 memory，否则必丢。
- 方案确认（用户裁决 2026-09-08）：**Remote-SSH 自动注册 Host（写 ~/.ssh/config 打标记块）+ 允许私钥导出到 ~/.ssh/bls-ops/**（Rust 侧写盘、unix 0600、不经前端）。
- 实现：
  - 新域 `src-tauri/src/commands/vscode.rs`：纯函数 `sanitize_alias`/`build_host_block`/`upsert_block`/`existing_block`/`block_matches`（幂等 upsert，同 alias 三元组一致复用、不一致找 `<slug>-2…`；同前缀 alias 防混淆）；命令 `vscode_open_remote_folder(state, server_id, path)`；6 个内嵌单测。
  - 复用 `editor_sync::find_editor("vscode")` 探测；Windows `.cmd` 经 `cmd /C` + **raw_arg 显式引号**（cmd 解析器不遵守 MSVCRT 规则，& 会断行）；`Code.exe` 直接 spawn。路径校验复用 `safe::validate_abs_path`（白名单天然挡引号/空格/&）；host/user 过 `validate_host_value`（禁控制字符/引号）。
  - 标记块格式：`# bls-ops:begin <alias>` … `# bls-ops:end`，幂等原地更新，手工可清理。Host key 不写 accept-new（沿用 VSCode/OpenSSH 默认 ask = 人工确认铁律）。
  - 前端：`api/types/vscode.ts` + `opsApi.vscodeOpenRemoteFolder(serverId, path)` + RemoteFilePanel 文件夹右键 "Open in VSCode"（仅保存过服务器且非 ProxyJump；quickTarget 无稳定 alias 不显示）；失败走 `useTransientNotice`。
- **重要发现**：`editor_sync` 域（远程文件/目录副本→本地编辑器打开→保存 SFTP 回传，含 locator 探测 VSCode/Cursor/Windsurf/Trae/CodeBuddy）**后端完整但前端零调用**——之前某会话的半成品，本次未接（与 Remote-SSH 是互补的两种模式，后续可做单文件编辑入口）。
- 并行会话提醒：验证时撞见 lib.rs 中间态笔误 `journal_query,s`（对方自己已修复）；**工作区有并行会话在改 terminal ghost/cd-completion**。
- 验证：cargo fmt ✓ / cargo test --lib vscode 6✓ / pnpm build ✓ / pnpm test 680✓ / cargo build ✓（exe 已重嵌）。
- 待用户实测：Remote-SSH 首连 VSCode 会弹指纹确认（设计如此）；密码凭据服务器连接时手输密码。
- 右键菜单 VSCode 项换品牌图标：新组件 `components/ui/vscode-logo-icon.tsx`（内联 vscode-icons `file-type-vscode` body，@iconify/react 离线渲染，多色 fill 不吃 text 色类）；`ContextMenuItem.icon` 类型放宽为 `React.ElementType`（lucide 照常兼容）。

## 终端命令块悬浮复制（2026-09-08）

- 需求：终端里输入命令+返回输出，鼠标悬浮整块高亮，点击复制命令/复制输出，报错块同样可用。
- 实现（全走受控边界，不猜）：
  - 新纯逻辑 `terminal-command-blocks.ts`：块模型（start/end 两枚 xterm `IMarker` —— **存 marker 不存行号数值**，回滚 trim 时行号整体前移，marker 自动跟随；line=-1=淘汰→块隐藏）；`beginBlock`（作废遗留未完成块+追加+超限挤最老）/`finishBlock`（封最后一个未完成）/`blockAtLine`/`bufferLineAtY`/`blockRectPx`/`isFailedBlock` 纯函数。
  - 接线全在 `use-terminal-results.ts`：`execute` 注册捕获 marker 同一时刻 beginBlock；`onResult`（D 标记已写完输出、提示符未回写时刻）registerMarker 封口，exitCode/renderedText 取自 CapturedResult（复制内容与结果抽屉一致）。`blocksRef` 真源模式（同 enhancedRef）；enhanced 关/unmount 时 `clearCommandBlocks` dispose。
  - 悬浮层 `TerminalCommandBlocks.tsx`：整层 pointer-events-none 只按钮可点（交互铁律）；mousemove→测量 `.xterm-rows` 行高→viewportY+行→命中→高亮矩形+块顶按钮条（复制命令/复制输出/红字 exit 徽标）；滚轮+resize 重算；alternate screen 不渲染（TerminalView 挂载条件 `!inAlternate`）；复制走就地 useCopyFeedback+CopyNotice。
  - i18n：`Copy output` 新增 zh-CN/zh-TW；`Copy command` 复用抽屉已有 key（**语言文件里同 key 不能重复出现**，TS1117）。
- **测试抓到真 bug**：`beginBlock` 原写 `slice(length - max)`，长度<max 时是负索引截尾丢块（14 个新测试中 2 个失败暴露）——切片挤出必须先算 `overflow=length-max`，>0 才切。
- 验证：pnpm build ✓ / vitest 694 全绿（新 14）✓ / cargo build ✓ exe 已重嵌。

### 用户报"完全没用"的排查（2026-09-08 傍晚）

- 用户在 `tauri dev`（vite 热更新）下测命令块悬浮 → 报完全没用。**先按铁律 diff dist/exe 时间戳——但用户在 dev 模式，此路不通**（devUrl 实时，dist 无关）。
- 静态排查：接线全在（execute→beginBlock、onResult→finishBlock、overlay 挂载条件、coordinatorRef 单实例）；`docker ps` 会捕获（blocksCapture=false）；enhanced 默认开；execute 依赖数组缺 applyBlocks 但全是稳定引用——**静态无断点**。
- 修复两处真实缺陷（虽非"完全没用"根因）：① 事件/几何基准统一到 wrapper（containerRef.parentElement）——复制按钮条在块矩形外，只监听 xterm 容器会被 mouseleave 闪没，永远点不到；② 鼠标在按钮条上时保持 hover（`data-block-actions` closest 守卫）。
- **组件测试钉死渲染层**：`TerminalCommandBlocks.test.tsx`（8 用例，项目模式=原生 createRoot+act+happy-dom，**无 testing-library**；getBoundingClientRect 用 spyOn prototype mock，按 className 区分 rows/host）。结论：同结构同几何下高亮/按钮/复制/报错徽标/淘汰块全正常。
- **收窄结论**：渲染层无 bug → 用户侧 commandBlocks 恒空 = 数据层没进来。二分法：敲命令看**结果抽屉**出不出来——没抽屉=enhanced 关（localStorage `bls-ops.terminal.enhanced`）或 HMR 旧闭包（重启 dev）；有抽屉没高亮才是真 bug。
- 教训：**用户报 dev 模式"没生效"时，第一时间问清楚 dev/prod，别按 dist 时间戳排查**；功能测试写"与真实接线相同结构"的组件测试是远程排障的最强手段。

### "悬浮没反应"根因定论（2026-09-08 第二轮）

- 22 个测试（纯函数 14 + 组件 8）全绿、组件测试用与 TerminalView 相同结构验证高亮/按钮/复制全正常 → 渲染层无 bug。
- **根因推断（唯一自洽解释）：vite Fast Refresh 保留旧闭包** —— `use-terminal-results.ts` 的 `execute` useCallback 依赖全是稳定引用（refs/sessionId），HMR 替换模块后 React 复用 `coordinatorRef.current` 里的旧 onResult 与旧 execute，**新代码从未被执行**。hook 是非组件模块，改动向上传播到 TerminalView 只触发 Fast Refresh 不触发整页 reload。
- 附带正确性修复：块起点 marker 改为**独立注册**（原与捕获 marker 共用，后者被 consumeTerminalSnapshot dispose 后不再跟随回滚 trim，输出多时块圈错行）。
- 用户侧最终验证路径：**完全重启 tauri dev**（热更新闭包必须整页刷新才清掉）；敲 docker ps → 等执行完（结果抽屉出现）→ 悬浮输出区。若重启后仍无高亮：F12（debug build）看 console 报错。

## 文本预览无法滚动（@uiw height="100%" 对 auto 父级失效，勿回退）

- 现象：普通文本文件预览打不开滚动条、滚不动（其他类型预览经 flex 修复已好）。
- 根因：`@uiw/react-codemirror` 的 `height="100%"` 只通过 theme `&` 打到
  `.cm-editor` 上，**外层 `div.cm-theme-*` 自身无高度（block auto）** →
  百分比对 auto 父级解析失败 → 编辑器被内容撑到自然高 → 被容器
  `overflow-hidden` 裁掉 → 无处滚动。FileEditorModal 用 `height="52vh"`
  （绝对单位）所以能滚，只有 CodeText（预览只读视图）踩中。
- 修复：`CodeText.tsx` 给 CodeMirror 传 `className="absolute inset-0"`
  （父容器本就 `relative`）→ 外层定高不再依赖 flex 百分比链；
  `height="100%"` 保留。CodeSearchBox 是独立 absolute z-30 覆盖层不受影响。
- 教训：**@uiw 的 height prop = cm-editor 的高度；想用 100% 必须自己保证
  外层 div 定高（h-full 或 absolute inset-0），库不会替你补**。
- 验证：pnpm build ✓ / cargo build ✓（exe 已重嵌）；preview 无组件测试。

## 单文件"在 VSCode 中打开"入口接线（editor_sync 前端零调用补齐）

- 背景：`editor_sync` 后端（副本下载→本机编辑器打开→保存自动 SFTP 回传）
  一直完整但前端零调用；文件夹走 Remote-SSH（`vscode_open_remote_folder`），
  普通文件此前没有 VSCode 入口。
- 接线（本次）：
  - `src/api/types/editor-sync.ts`（新）：EditorInfo / SyncSessionInfo /
    EditorSyncEventPayload，与 Rust model camelCase+snake_case 枚举逐字对齐。
  - opsApi 四条：`editor_list_available` / `editor_sync_open(sessionId,
    remotePath, editorId)` / `editor_sync_close` / `editor_sync_list(sessionId?)`
    （Rust Option 参数传 `?? null`）。事件名 `editor-sync-update` 进
    `lib/events.ts::editorSyncEvent`。
  - RemoteFilePanel：挂载时探测一次 vscode 是否 installed（`available`）→
    文件右键 Preview 和 Download 之间插 "Open in VSCode"（未装不显示）；
    点击前先 `editorSyncList` 查同路径 starting/active 会话（防双开双副本），
    有→提示 "Already open in VSCode"，无→open + 成功提示（保存自动回传）。
  - 面板内 `listen(editorSyncEvent)`：该 session 的 `status=error` →
    transient notice "Save sync failed: {{message}}"（保存失效必须可见，
    下次保存成功后端自动回 active）。
  - i18n：zh-CN/zh-TW 各加 3 条（Already open in VSCode / Opened {{name}}
    in VSCode — saving syncs back to the server / Save sync failed: {{message}}）。
  - 该模式走面板自己的 SSH 连接（非 serverId），quick target 也能用；
    与 Remote-SSH（要稳定 alias）互补。
- 顺手修并行会话遗留：`TerminalCommandBlocks.test.tsx` mock
  `vi.fn(async () => true)` 无参签名 → TS2554，改显式 `(_text: string)`
  （与既有 vi.fn 参数约定同族）。
- 验证：pnpm build ✓ / pnpm test 52 文件 727 条 ✓ / cargo build ✓ exe 已重嵌。
- 未做（后续可加）：同步会话管理 UI（列表/手动关闭 editor_sync_close）、
  文件夹 scope 的本地同步模式、非 VSCode 编辑器选择入口。

## 终端连接状态行：i18n + 绿色 + 简短化（用户要求，2026-09-08）

- 用户反馈：连接成功行 `已连接 host:port（ED25519）` 未国际化且信息太啰嗦，要求简短一行、绿色显示。
- `TerminalView.tsx` connect 成功分支：改为 `\r\n\x1b[32m${t("Connected")}\x1b[0m`——只写一行绿色"已连接"，去掉 host:port/fingerprint_type（Connected 在 common.ts 已有全语言 key，不重复添加）。
- 同文件连接失败分支 `连接失败：${message}` 同类硬编码，顺手国际化为 `${t("Connection failed: {{message}}", { message })}`。
- key 补充：zh-CN/zh-TW terminal.ts 各加 `"Connection failed: {{message}}"`；两文件头部注释修正（旧注释称"发给 xterm 的字符串不翻"，现明确：前端自绘连接状态行走 i18n，仅远程输出/结果快照不翻）。
- 验证：read_lints 0 错误；pnpm build ✓；随后 touch build.rs + cargo build 重嵌 dist。

## README 精简（2026-09-08）

- 用户要求：README 只介绍重要功能和下载地址。已重写为 ~30 行：一句话定位 + 核心功能 13 条（SSH 终端/服务器管理/监控/服务/日志/Docker/Nginx/远程文件/命令中心/增强终端/项目部署/安全边界/多语言）+ 下载地址（https://github.com/npcxl/BLS-OPS/releases，注明内置自动更新）+ MIT。
- 删除：技术栈表、快速开始、实现进度表、项目结构、IPC 契约、测试详情、阶段验收清单、安全模型/数据真实性章节等全部开发向内容。
- 版本号未写死（package.json 当前 0.1.6，下载页动态可见）。

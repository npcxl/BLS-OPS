# 2026-09-02

- 应用户要求删除 `ModuleFrame`（P3 模块公用框架）顶部工具栏右侧的常驻连接状态角标（小圆点 + 已连接/连接中/连接失败/已断开/未连接 文字），用户认为很多余。同步删除 `PHASE_TONE`/`PHASE_LABEL` 常量与 `Server as ServerIcon` 导入；connecting/error/closed 三种需要操作的横幅提示保留。`tsc --noEmit` 通过。
- MEMORY.md 已按注入截断提示完成精简重写（去重、压缩 P3/监控/历史问题段落，结论全部保留）。

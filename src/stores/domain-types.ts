/**
 * Domain types — spec §8, §35–§102.
 *
 * Backward-compatible re-export barrel. All entity definitions now live in
 * `./domain/<module>.ts`; import from `@/stores/domain` (or the specific
 * module) going forward.
 */

export * from "./domain";

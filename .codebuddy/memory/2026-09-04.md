# 2026-09-04

## 文件夹图标改 Windows 黄（用户反馈"文件夹太暗"）
- vscode-icons 的 `default-folder` 是暗蓝灰色，在应用底色上几乎不可见。用户要求"像 Windows 一样"。
- 考察：fluent-color `document-folder` 是紫色系不用；fluent-emoji `file-folder`（3D 风）与 **fluent-emoji-flat `file-folder`**（扁平）都是 Windows 黄——flat 版配色 `#ffb02e`+`#fcd53f`，32 viewBox，小尺寸（14px 行内）更清晰 → 选 flat。
- 改动：`scripts/generate-file-icons.mjs` 的 COLLECTIONS 加 `fluent: @iconify-json/fluent-emoji-flat`，`folder` 候选链改 `["fluent:file-folder", "default-folder"]`；重跑 `pnpm icons:regen`；`@iconify-json/fluent-emoji-flat` 进 devDeps（仅生成期）。
- **取舍**：只换默认 `folder`；特殊文件夹（folder-git/node/docker 等）保留 vscode-icons 徽章图标保辨识度——用户若嫌暗可再统一。
- Iconify 对象数据带 width/height=32，但 EntryIcon 显式传 width={14} 覆盖，无影响。验证：pnpm build 6.66s ✓。
- 踩坑复发：PowerShell 内联 `node -e` 正则转义再次解析失败且**导致同命令行里 pnpm add 也没执行**（`cmd1; node -e ...` 解析失败=全行不跑），复杂校验一律临时 .mjs。

## 服务器列表收藏/分组统一重构（fa747b0 之后）
- **后端早已具备**，`server_move_to_group`、`set_server_favorite` 的 UPDATE affected-rows 校验、分组同名校验（db `insert_or_replace_server_group` + 命令层 `validate_group_name`）都在，db/tests.rs 已有 5 个对应单测。本次只做前端接线。
- 新增 `src/workbench/server-list/` 作为**唯一实现**：`sections.ts`（纯逻辑，groups 为主数据 → 空分组可渲染；`group_id` 指向不存在的分组归入未分组并产生 warning）、`use-server-list.ts`（乐观更新+回滚+错误）、`ServerRow` / `ServerGroupSectionView` / `ServerListTree` / `SectionTitle` / `ServerForm` / `NewGroupInput`。
- **两个侧栏退化成壳**：`ssh-context-sidebar.tsx` 与 `module-server-sidebar.tsx` 各自只剩标题 + `onOpenServer`（约 30 行）。`ServerForm` 从 ssh-context-sidebar 移出，`WorkbenchHome.tsx` 的 import 已改指新路径。
- ops-api：`setServerFavorite` 返回类型由 `void` 改为 `ServerRecord`（后端本来返回记录）；新增 `moveServerToGroup(id, groupId)`。store 加 `moveServerToGroup`。
- 删除分组从 `window.confirm` 改为 `ConfirmDialog`（违反 MEMORY 硬约定，已修）。
- `ContextMenu` 新增**一层子菜单**（`children`）：hover/点击/→ 打开，← 与 Escape 逐级关闭，超出视口向左翻转。"移动到分组"用它。
- 测试：新增 `sections.test.ts`(11) + `ServerListTree.test.tsx`(17) + context-menu 子菜单(7)。`ServerListTree.test.tsx` 用 `vi.mock("@tauri-apps/api/core")` 假装后端，跑真实 store+API 链路。pnpm test 262 全过、pnpm build ✓。
- **cargo check / cargo test 均失败**，但**与本任务无关**：`src-tauri/src/output_adapter/generic/table.rs:207` `E0277`（`Vec<&&String>` 应为 `Vec<&String>`）属并行会话的 output_adapter 中间态（该文件 +216 行）。按 MEMORY 约定"对方中间态文件别代改"，未修，已向用户报备。
- 测试踩坑：`vi.hoisted` mock 后组件挂载的异步 `load()` 需要 `await act(async()=>{render})` + `setTimeout(0)` 二次 flush 才稳定；受控 input 用 `HTMLInputElement.prototype` 的 value setter + dispatch `input`；React 的 `onPointerEnter` 由原生 `pointerover`（bubbles:true）派生，直接 dispatch `pointerenter` 不触发。

## 顶栏展开入口 + 右键功能补全（同日第二段）
- **顶栏展开按钮**：侧栏收起后其自带"收起"按钮跟着消失，原先无法再展开（只能切模块绕回）。AppTopBar 加 `PanelLeftOpen` 按钮，条件 `collapsed && hasContextSidebar(activeModule)`；`hasContextSidebar` 在 module-server-sidebar.tsx（ssh + SERVER_LIST_MODULES），设置/命令/部署等无栏模块不显示。按钮必须 `data-tauri-drag-region="false"`（否则被窗口拖拽区吞掉点击）。测试 AppTopBar.test.tsx(7)。
- **右键 = 顶部功能**原则落地三处 + 列表空白区：
  - 服务器列表空白区右键 = 头部图标行（刷新/新增服务器/新增分组/收起侧栏），`headerActions` 数组单一来源两处渲染；行级菜单 stopPropagation 由 useContextMenu 的 `event.stopPropagation()` 天然隔断。ServerListTree.test.tsx 增 3 个用例。
  - **命令结果面板 CommandResultPanel**：结构化/原始切换 + 复制原始输出/实际命令/完整结果；`canStructure`（view 为 raw/text 时禁用结构化项）与 header 分段控件同逻辑。测试 7 个。
  - **日志中心表格行**：复制该行（时间+级别+单元+消息 双空格拼接）、复制全部、只看该级别（=当前禁用标"当前"）、只看该单元、搜索该消息、清除筛选——全部复用既有 toolbar state（setUnit/setPriority/setSearch），不新增 state。测试 6 个（需 mock `listen` + `ssh_connect_monitor` 返回 connected + journal_query 按 unit 过滤模拟服务端行为）。
  - **工作台首页卡片 ServerHomeRow**：打开终端/收藏/编辑/复制连接地址/删除；卡片原先只有收藏+删除 hover 图标，编辑/复制不可达。`ServerHomeRow` 增加 `onEdit` prop。测试 5 个（直接 seed useDomainStore，WorkbenchHome 不自带 refresh）。
- 新增 `src/lib/clipboard.ts` `copyText()`（统一 try/catch 返回 boolean）；后续新复制功能用它，别再散落 navigator.clipboard。
- 测试铁律补充：`CommandExecutionResult.structured` 声明是 adapter 窄类型 `CommandStructuredOutput`（无 view），但运行时/parseStructuredResult 吃的是完整 `StructuredCommandResult`（含 view/meta/raw）——测试 fixture 需 `as unknown as` 断言；**类型不一致是既有问题**，属 output_adapter 会话管辖。
- tsc 的 TS2550 `.at(-1)`：项目 lib 目标 < es2022，用 `arr[arr.length-1]`。

## 同一任务的双会话协作（本会话=后端，并行会话=前端，零冲突）
- 用户把"服务器列表收藏与分组修复"同时发给了两个实例。本会话先完成后端（db+命令+测试），发现工作区有并行前端改动后**按文件分工**：不碰对方正在写的 `server-list/` 组件与两个 sidebar，只做互补——先写 `sections.test.ts`（10 用例，后被对方扩充到 11）。
- 本会话后端产出：`set_server_favorite`→`Result<Option<ServerRecord>>`（0 行=不存在）；`move_server_to_group`；`insert_or_replace_server_group` 同名 bail；命令层 `validate_group_name` + `server_move_to_group`（存在性校验+审计）+ lib.rs 注册；db/tests 4 新测 + commands/tests 3 新测。
- 终验（10:08）：`cargo test --all-targets` 322 lib + 19/25/27 e2e 全过；vitest 262 全过；pnpm build ✓；cargo build 重嵌 dist ✓。fmt 剩余 diff 全在并行任务文件（command_center/ssh/output_adapter）。
- 经验：并行会话改动靠 `git status` + 文件 LastWriteTime 轮询判断活跃度；对方改了我的 sections.test.ts 说明对方能看到本会话产出，互补写测试是安全切入点。rustfmt 对链式 `.unwrap().unwrap()` 超宽强制拆行，新测试直接写成三行链。

## 验收全绿快照（同日 11:00 后）
- pnpm test 308（28 文件）全过；pnpm build ✓；cargo check --all-targets ✓（仅警告）；cargo test --all-targets ✓（322 lib + 19/25/27 e2e）；cargo fmt --all -- --check ✓；cargo build 重嵌 dist ✓。
- 教训：**pnpm build 首次报 TerminalView.tsx:22 TS6192 属并行会话编辑中间态**——我第一遍 grep 时文件里 ContextMenu 用法确实缺失，重跑 build 前 tsc 又变绿（对方已补回 920/921/1211 行用法）。单次 tsc 失败不要急着代改对方文件，先重跑/`tsc --noEmit` 复验。
- 遗留警告（非门禁）：command_center/tests.rs 未用 import（ExecKind、RiskLevel、CommandCategory）、`catalog.clone()` noop；db.rs `P3_SCHEMA_SQL` 未用；`deployment_from_row`/`deployment_list` dead_code；`DEPLOY_RUNNING` 未用等 33 条。均属 P4/并行重构产出，别主动清。
- fmt 门禁记得先 `cd src-tauri`（根目录无 Cargo.toml）。

## 终端两个小改（用户最后一轮：仅此而已，其它 tab 别动）
- **终端画布右键 = 顶部 icon 工具栏镜像**：`TerminalView` 加 `useContextMenu`；菜单绑在 xterm 宿主容器 div 的 `onContextMenu`（= `openToolbarMenu`，渲染时构建 items）。菜单项：查找/垂直分栏/水平分栏/清空屏幕/命令历史/远程文件 + 条件项"结构化结果"（有结果才出现）+ 分隔 + 连接动作按 `phase` 二选一（connected→断开连接 danger / 其他→重新连接，connecting 时 disabled）——可见性与 toolbar 完全一致，不复用 toolbar JSX（顶部含 select/搜索框等非命令控件，无法数组化）。toggle 类项用 `hint:"已展开"` 标当前态。
- **踩坑**：打开菜单必须先把 xterm 隐藏 textarea `blur()`，否则菜单 ↑↓/Enter 键盘导航被终端 attachCustomKeyEventHandler 吞掉发给 shell。
- **提示面板翻转遮挡输入修复**（docker 提示翻到上方盖住正在敲的命令）：`SuggestAnchor` 加可选 `rowHeight`；`computeSuggestPosition` 底部翻转由 `anchor.y - gap - h` 改为 `anchor.y - (rowHeight??0) - gap - h`（让开光标所在整行，输入始终可见），无 rowHeight 走旧行为；`TerminalView.updateSuggestAnchor` 把 `cellHeight` 写入 anchor 的 `rowHeight`。`ParamPicker` 直接贴 anchor 不下翻、不遮挡输入行，未动。
- 测试：terminal-suggest.test.ts +2（让行翻转 + rowHeight clamp）共 13；pnpm test 310 全过（AppTopBar act 警告为既有）；pnpm build ✓。用户改前端后需 `cargo build` 重嵌 dist 才在 tauri 生效（本次未跑 cargo）。

## 服务器列表右键扩展到整列（用户："只在内容盒子里能右键，应该整列都可以"）
- 根因：空白右键只绑在 ServerListTree 内层"内容盒子"（`mt-1` 的标题+列表 div）；滚动容器 `.min-h-0 flex-1 overflow-y-auto`（ContextSidebar）下内容不足时，列底部空白在该盒子之外 → 无右键。
- 修复：把 `onContextMenu`（headerActions 背景菜单）从内层 div 上移到 ServerListTree 根 div，根 div 加 `min-h-full` 撑满侧栏可滚动区整列；行级菜单仍靠 ServerRow 自身 stopPropagation 优先。加了 `data-testid="server-list-tree"`，测试 +1（21 全过）。
- 经验：滚动容器内让首个子元素占满可视高的惯用写法 = 子元素 `min-h-full`（容器 flex-1 高度 definite 即可解析）；Tauri WebView2(Chromium) 支持。

## 服务器列表分组数量数字贴整行最右（同日第三段）
- 分组 header（ServerGroupSectionView）原本：数量在 toggle 按钮内被名称 flex-1 推到按钮右缘，但按钮外还有 hover 才可见的重命名/删除操作区（opacity-0 仍占位）→ 数量其实离行最右空了一截。
- 修复：把 `group-count` span 从 toggle 按钮内移到**整行最右**（hover 操作按钮之后），数量恒定贴右，hover 按钮在它左侧浮现。toggle 按钮只剩 chevron+名称。收藏 header 数量原就无操作区、已最右，未动。测试 21 全过，无类型改动。

## ContextMenu 全局单例互斥（用户：行菜单开后再空白右键两个菜单并存）
- 根因：每个右键区域是独立 `useContextMenu` state。行菜单 A 开着时右键空白，背景 handler 打开菜单 B——A 的 window contextmenu 监听看 `defaultPrevented` 以为"会被替换"而不自关，于是两个 menu portal 并存。
- 修复（context-menu.tsx）：模块级 `activeMenuClose` 槽；ContextMenu 打开时注册自己的 close（effect `[open]`），关闭时释放；`useContextMenu.onContextMenu` 打开前先 `closeActiveContextMenu()`。同实例连续右键 = close+open 被 React 批处理成一次移动，仍不闪烁（原测试 "survives...never flickers" 通过）。跨模块所有使用点全局生效。
- 测试：context-menu.test.tsx 新增 TwinHarness 互斥用例（20→21）；ServerListTree.test.tsx 新增"行菜单→空白右键只剩背景菜单"（21→22）。
- 全量 pnpm test 2 个失败均在 TerminalCommandCoordinator.test.ts（期望第一批输出后 results 空却得 1 个）→ 归属并行 OSC133 快照重构（coordinator .ts/.test 相对 HEAD 各改 272/130 行），非本次改动；**pnpm build 报 TerminalView.tsx TS 错误**（IMarker/RenderOutcome/extractTerminalSnapshot 未用 + `adapt` 不在新 CoordinatorDeps）—— TerminalView 正被并行会话改造，勿代改，已报备。

## 终端 OSC133 快照重构收尾（验收补完，11:45）
- **协调器早发 bug 根因（关键经验）**：`tryEmit` 只查 `matchPending/renderPending` 不查 `done` → match 微任务先回来（慢命令还在输出）就把"进行中命令"当"已完成"提前 emit 空/降级结果 → "慢命令两批/超 10s 输出"两用例挂。修复：`tryEmit` 首行守卫加 `if (!session.done || session.matchPending || session.renderPending) return;`。终端目录 73 测试全绿。
- TerminalView 接入复核通过：coordinator 只传 `match + onResult + captureNow`；`executeTerminalCommand` 在 `plan.capture` 时 release 旧 marker 后 `instance.registerMarker(0)`（返回值可能 undefined → 存 `marker ? {marker} : null`）；stdout 按 `parsed.parts` 区分 sawEnd 前后文本，`queueWrite(display(before))` promise 完成后 `consumeTerminalSnapshot` 再 `provideRenderedText`，after 在快照后写。
- TerminalSnapshotView/TerminalResultDrawer 复核达标：渲染 tab 是 `<pre className="w-max whitespace-pre">` 不折行 + overflow-auto 横滚；原始输出走 RawStreamView(stdout/stderr)；降级横幅 TriangleAlert + "已从原始输出降级…软换行无法还原"；复制走 `copyText`；内容区 `<TerminalSnapshotView key={active.id} />`。
- **非终端路径确认未受影响**：`CommandResultPanel/ResultPanel.tsx` 仍服务 `command-center` 知识库执行（`CommandExecutionResult` 结构化协议）——正确保留；`commandAdaptOutput` API 定义保留但**零调用方**（符合"终端不再走适配器"）。修正两处过时注释（TerminalView runSuggestion、TerminalSuggest 顶部：adapt_auto → 渲染快照）。
- 终验：pnpm test 324 全过（29 文件，AppTopBar act 警告既有非失败）；pnpm build ✓（tsc+vite，仅既有 circular chunk 警告）；read_lints 0 错。

## P4 终端/模块链路拆分验收补完（提交 7efa21e，本地已领先远端 3cf61a4 两个提交）
- **重要事实核对**：用户评审基于**远端 3cf61a4**（sandbox 引用 auto.rs/JsonView/CommandResultPanel），但本地 408e33a 起就已把终端从适配器链路拆出（`commandAdaptOutput` 定义保留但**零调用方**；协调器只吃 xterm 快照，不再 `readableOutput`/`as unknown as never`）。本轮按用户最终方向补齐仍缺的验收项，未在旧 auto 适配器上打补丁。
- **严格 JSON/JSONL 前端检测**（新增 `src/lib/detect-json.ts`，数据完整铁律）：整段 trim 后整体 `JSON.parse` 成功 → json；否则 JSONL **逐非空行解析，任意一行失败整体 `null`**，禁止"跳过坏行部分成功"；空/超 1MB → null。`detectJsonOutput(text, command)`：快照首行是 `prompt+命令回显` 时先剥该行再检测——**只决定 JSON Tab 出不出，绝不改终端输出本身**。13 单测。
- **终端结果模型收口**（`TerminalCommandCoordinator.CapturedResult`）：删 `rawOutput` 兼容别名（原来与 stdout 完全重复），新增 `json: DetectedJson | null`，emit 时在 `truncated=false` 下对渲染文本做严格检测。终端链消费方只读 `stdout`。
- **TerminalSnapshotView 三 Tab**：`[终端输出]`(默认=快照 `<pre w-max whitespace-pre>`)`[JSON]`(仅 `json!==null` 出现)`[原始流]`(RawStreamView(stdout,stderr))；降级横幅保留。JSON Tab 内容=JsonView。
- **JsonView 彻底重写**：可折叠 JSON 树（默认展开，行 hover 复制路径/复制节点 JSON）+ 文本模式 + 搜索（命中展平列表，最多 500）+ 全部折叠/展开 + 复制整段；**删除数组→表格分支**（用户裁决：JSON 只展示、绝不二次表格化）。注意 `renderer.test` 断言 `"jsonKey": 1` 依赖叶子行 `键 + "{': '}" + 值` 的 ASCII 空格拼接（`&nbsp;` 会挂测试）；路径段 `.key`/`[i]`/`["…"]`。模块链 command-center 的 JSON 视图同样受益，Table 只由 TableView/表格适配器产生。
- **Rust 同步收紧**（`output_adapter/generic/json.rs`）：`parse_json` 的 JSONL 从 `.filter_map(跳过坏行)` 改为**逐非空行解析、坏行整体返回 None**；删除死函数 `json_array_to_rows`（JSON 数组→表格，已被用户否决）；registry 失败原因改“含非 JSON 行…保证完整”。`json_parser_handles_single_and_strict_lines` 测试同步更新。
- **协调器测试 +5**（嵌套 describe“严格 JSON Tab 检测”）：整段 JSON→json 命中且渲染文本原样；prompt+回显行只影响检测不影响渲染；JSONL 每行合法→jsonl 全保留；混入 warning 行→`json:null` 原样全保留；普通文本→null 默认终端输出。
- **终验**：pnpm test **342 全过**（30 文件）；pnpm build ✓（10.88s）；cargo test 27 lib 全过；cargo check ✓；cargo fmt ✓。提交 7efa21e（9 文件 +679/−134）。用户正式验收前需 push；本地 origin/main 跟踪已含 408e33a。

## 终端工具栏：新增"增强终端"开关 + 删除"编码"栏目（用户裁决）
- **增强终端开关**（工具栏 `ToolbarIcon`，图标 `Sparkles`，与查找/命令历史/远程文件同一组件与 `active` 高亮语义，右键画布菜单同步加一项带 `hint:"已开启"`）：**默认关**。关着 = 纯终端——`planCommandSubmission(cmd, mode, { capture: false })` 直接返回 `capture:false / markers:[] / write:"命令\n"`，**一个受控标记都不注入**；命令照常发往 shell。打开后才会注入 OSC133 标记、捕获输出、出现"结构化结果"按钮与结果抽屉。
- 三处门禁缺一不可：① 提交计划 `{ capture: enhancedRef.current }`（用 ref，协调器/提交回调都读到当前值）；② 协调器 `onResult` 首行 `if (!enhancedRef.current) return;`（丢掉飞行中捕获的迟到结果）；③ 抽屉渲染 + "结构化结果"按钮/菜单项都加 `enhancedTerminal &&`。关闭开关时清空 `commandResults/activeResultId` 并 `drawerClosed=true` → 真的"什么都没有"。
- 持久化：`localStorage` 键 `bls-ops.terminal.enhanced`（"1"/"0"，try/catch 兜底隐私模式），切换终端 tab / 重启后保持。
- **删除编码栏目**：移除工具栏"编码" select 及 `encoding` state、`sshGetEncoding`/`sshSetEncoding` 前端调用、`TERMINAL_ENCODINGS/TerminalEncoding` 与 `ChevronDown` 导入。**后端 Rust 会话编码（`ssh_set_encoding` 等）保留未动**——后端默认仍是 auto（UTF-8→GB18030 降级），只是不再给用户暴露切换入口。
- 测试：command-plan.test 增 2 例（capture:false 的 full/line-ready 都不发标记）。pnpm test 344 全过；pnpm build ✓（8.03s）。

## P4 清理轮（用户评审 7efa21e 后的收尾，未提交）
- **删除明确残留**：前端 `commandAdaptOutput`（ops-api，零调用）；Rust `command_adapt_output` 命令 + lib.rs 注册（`adapt_auto` 仍被 `command_execute` 用，注释/测试措辞改为 adapt_auto，测试保留）；`command-center/ContainerTable.tsx`、`StructuredTables.tsx`（无调用者的旧专用表格）；`command-result/views/ReadableOutputView.tsx` + `CommandResultPanel` 的 `readable` 参数/分支（模块链仍走 RawView）。**保留**：`output_adapter/`、`CommandResultPanel`、`CommandResultRenderer`、各模块视图、`adapt_auto`。
- **文案统一**：终端工具栏/右键菜单 `结构化结果` → `命令结果`；过时注释全部改写（结果产生与是否命中知识库无关，只与"可捕获"有关）。
- **两个细节修复**：① stderr 改走 `queueWrite`（与 stdout 同一条渲染队列，晚到的 stderr 也能进本次快照）；② 选区菜单改用算好的 `style={{left, top}}`，去掉写死的 `left-1/2 -translate-x-1/2 top-3`。
- **字体统一**（用户建议）：tokens.css 新增 `--font-terminal` / `--font-command-output`（Cascadia Mono → Sarasa Mono SC → JetBrains Mono → Consolas）；xterm 用 `getComputedStyle` 读 `--font-terminal` 设 `fontFamily` + `lineHeight:1.25`；结果快照 pre 与 JsonView 文本模式用 `var(--font-command-output)`（text-12）。UI 标题/按钮仍用 Inter（--font-ui）。**未打包字体**（体积），机器没装就回退。
- **新增测试**：`TerminalSnapshotView.test.tsx`(8) 与 `JsonView.test.tsx`(9)（默认终端输出、长行不折行、JSON Tab 仅在合法时出现、坏 JSONL 无 Tab、原始流控制字符转义+复制原始字节、降级提示、空输出；数组不转 table/th、折叠展开、搜索过滤、复制路径=JSONPath、复制整段、文本模式、根原始值）。
- **TerminalView 拆分的第一个成果**：抽出 `terminal-output-pipeline.ts`（`splitAtOutputEnd` + `writeOutputParts`，只依赖 write 的 promise，禁 setTimeout 猜渲染）+ 7 个测试，覆盖"D 之后的提示符不进快照、两批输出只有第二批抓快照、D 单独成块先 flush、空 before"。TerminalView 相应内联逻辑已改为调用它。
- **终验**：pnpm test **368 全过**（33 文件）；pnpm build ✓（7.23s）；cargo test 322 lib + 19/25/27 e2e 全过；cargo check ✓（33 警告=既有基线，`unused variable: ctx` 在 registry.rs:127 属既有）；cargo fmt ✓。**改动未提交**（等用户验收）。
- **待办（用户建议、本轮未做）**：TerminalView.tsx 继续拆（`use-terminal-instance/io/command-capture/search/selection` + `TerminalToolbar.tsx`）；大测试文件拆分（db/tests 909 行、output_adapter/tests 493、ServerListTree 485、协调器 376、context-menu 399，公共 fixture 抽 `terminal-test-utils.ts`）。

# P5.1 桌面应用自动更新

只用 Tauri 官方 Updater（`tauri-plugin-updater` + `@tauri-apps/plugin-updater`），
第一版只提供 **stable 单通道 + GitHub Releases 静态更新源**。

## 1. 执行链路

```
启动 8s 后（仅一次 / 仅生产构建 / 可在设置里关闭）
   └─ useAutoUpdateCheck()                 src/hooks/use-updater.ts
        └─ useUpdaterStore.check()         src/stores/updater-store.ts
             └─ UpdaterClient.check()      src/lib/updater/updater-client.ts
                  └─ plugin check()  ──HTTPS──> GitHub Releases latest.json
                                              （含 version / notes / url / signature）

用户点“下载并安装”
   └─ useGuardedUpdate().requestInstall()  src/workbench/updater/use-guarded-update.ts
        ├─ 第 1 次守卫：有活动 SSH / 传输 / 未保存文件 / 长任务 → UpdateRestartDialog
        └─ store.install()
             ├─ client.download(onProgress)        → phase downloading + 真实进度
             ├─ 第 2 次守卫（下载完成后再查一次）   → 有新活动则停在 downloaded
             └─ client.install()                  → phase installing
                  └─ 插件校验 minisign 签名；失败即中止，前端无“仍然安装”入口
   └─ phase = restart_required → “立即重启” → process.relaunch()
```

状态机集中在 `useUpdaterStore`：
`idle / checking / up_to_date / available / downloading / downloaded /
installing / restart_required / cancelled / error`。
组件只读写 store，不直接调 `check()`。

**为什么有 `downloaded`**：下载可能持续几分钟，下载开始前查过一次活动状态不等于
安装时仍然安全。第二次守卫在真正跑安装程序之前再查一次；一旦发现新的会话、传输、
未保存文件或长任务，流程就停在 `downloaded`，等用户确认后再装——包已在磁盘上，
稍后继续不会重新下载。

## 2. 关键约束（勿回退）

- 签名校验常开：公钥在 `src-tauri/tauri.conf.json → plugins.updater.pubkey`，
  由 `generate_context!` 编进二进制；**没有关闭开关**，也没有“仍然安装”。
- 前端不 `fetch` 安装包、不用 shell 拉起 exe/msi，下载与安装全部由插件完成。
- CSP 未放开：更新请求由 Rust 侧发起，不需要前端 `connect-src`。
- 最小权限（`src-tauri/capabilities/default.json`）：
  `updater:allow-check` / `updater:allow-download` / `updater:allow-install` / `process:allow-restart`。
- 更新签名（minisign，验证"这个包是我们发的"）与 Windows Authenticode
  代码签名（验证"这个 exe 是谁发的"）是两件事，互不替代。当前阶段**未做**
  Authenticode，SmartScreen 仍会提示未知发布者。
- JS 与 Rust 插件版本同号且用 `=` 钉死（`scripts/check-versions.mjs` 会挡住漂移）。

## 3. GitHub Secrets 清单（只列名，不含值）

| Secret | 内容 | 来源 |
| --- | --- | --- |
| `TAURI_SIGNING_PRIVATE_KEY` | Updater 私钥全文（minisign） | `pnpm tauri signer generate -w <file>` |
| `TAURI_SIGNING_PRIVATE_KEY_PASSWORD` | 该私钥的口令 | 生成时设定的口令 |

- `GITHUB_TOKEN` 由 Actions 自动提供，无需手动配置。
- 私钥与口令**只存在于 GitHub Secrets 与本机的 `.tauri/`（已被 .gitignore 忽略）**，
  仓库里只有公钥（`.tauri/updater.key.pub` 同样被忽略，公钥本体在 tauri.conf.json）。
- 工作流启动时若两个 Secret 为空会直接失败，不会产出无签名的包。

## 4. 发布步骤（全自动）

正常发版只需要一步：

```
GitHub → Actions → Release → Run workflow
   bump: patch | minor | major     tag: 留空
```

工作流会依次完成：

1. `pnpm version:bump <level>` 以 `package.json` 为唯一输入，同步写入
   `src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json`、`src-tauri/Cargo.lock`。
2. `pnpm install --lockfile-only` 刷新 `pnpm-lock.yaml`（若变化则一并提交）。
3. `scripts/check-versions.mjs` 校验四处版本 + 前后端插件版本一致，不一致即失败。
4. 提交 `chore(release): vX.Y.Z`、创建并推送 `vX.Y.Z` tag。
5. 前端 lint/type/build/test + Rust fmt/clippy/test。
6. `tauri-apps/tauri-action@v1` 构建、用私钥签名、产出 `.sig` 与 `latest.json`，
   上传为 **draft release**。
7. 人工检查 draft 后点 **Publish release**。

要点：

- 版本号由 `0.1.0` 自动推导（`patch → 0.1.1`、`minor → 0.2.0`、`major → 1.0.0`），
  不再手工改四处文件。
- **一次工作流内完成 bump / 提交 / 打 tag / 构建**：用 `GITHUB_TOKEN` 推送的提交
  不会触发其它工作流，拆成两个工作流会导致新 tag 永远不会被构建。
- 只想给当前版本补一个 tag（例如发 `0.1.0` 基线）时，填 `tag: v0.1.0`，
  `bump` 会被忽略，**不会**改动版本号。
- 直接 `git push origin vX.Y.Z` 也可触发，此时不做 bump，只按 tag 的版本构建。
- 只有 published release 才会出现在 `/releases/latest/download/latest.json`，
  draft 不会变成线上更新。

本地等价命令（不推送时用）：

```bash
pnpm version:bump patch          # 或 DRY_RUN=1 预演
pnpm check:versions
pnpm build && pnpm test
cd src-tauri && cargo fmt --all -- --check && cargo test --all-targets
```

## 5. 自动检查策略

| 规则 | 实现位置 |
| --- | --- |
| 启动后延迟 8s | `AUTO_CHECK_DELAY_MS` |
| 每次启动最多一次 | `checkedThisLaunch` |
| 开发环境不检查 | `isDevRuntime()`（`import.meta.env.DEV`） |
| 失败不影响启动 | `check()` 内部吞掉异常，只写 store |
| 自动检查无更新不弹提示 | `manual=false` 时 phase 回到 `idle` |
| 自动检查发现更新 → 非阻塞卡片 | `UpdateNotification` |
| 不自动下载/安装/重启 | 只能由用户点按钮触发 |
| 下载前 + 安装前各检查一次 | `updater-store.ts::blockingActivity()` |
| 阻断项清单 | `restart-guard.ts` + `activity-store.ts` |

## 6. 测试覆盖

| 文件 | 覆盖 |
| --- | --- |
| `src/lib/updater/test/errors.test.ts` | 11 类错误分类 + 日志脱敏（Windows/Unix 路径、token、签名 blob） |
| `src/lib/updater/test/restart-guard.test.ts` | SSH 会话、未保存文件、传输、命令、长任务的阻断聚合 |
| `src/stores/test/updater-store.test.ts` | 无更新 / 有更新 / 进度 / 重复检查 / 重复安装 / 离线 / 超时 / 缺平台 / 无效签名 / 等待重启 / 重启失败 / 不重复下载 / **下载期间新会话→停在 downloaded** / **从 downloaded 续装不重下** / 0.1.0→0.1.1 / 开发模式不下载 |
| `src/workbench/updater/test/UpdateSection.test.tsx` | 版本/通道/上次检查渲染、活动会话阻断、未保存文件提示、取消后仍可稍后重启、进度渲染、错误可读 |

## 7. 尚未支持 / 限制

- **仅 Windows x86_64 完成更新链路验证**；macOS / Linux 在
  `.github/workflows/release.yml` 中保留矩阵扩展位，但 macOS 需要 Developer ID
  签名 + 公证、Linux 需要逐发行版验证，未做之前不得宣称支持。
- 无 beta / nightly / 灰度 / 多通道，无自建更新服务器。
- 无增量补丁、无自动回滚、无强制更新。
- 无 Authenticode 代码签名（与更新签名无关）。
- 更新过程中不会主动断开 SSH：有活动状态时只会弹确认，取消即保持
  `downloaded` 或 `restart_required`，稍后可继续且不重新下载。

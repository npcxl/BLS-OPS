

````md
# 任务：在不改变现有逻辑的前提下进行模块化重构

请先完整检查当前工作区中的最新代码，再开始工作。

重要说明：

- 当前工作区代码是唯一事实来源。
- 不要以历史提交、旧文档、旧版代码或之前的分析结果为完整依据。
- 项目最近新增了代码，已有架构信息只能作为参考。
- 本次任务只做模块化重构，不新增功能、不改变业务规则、不修改产品交互。
- 如果发现明显 Bug，先记录，不要借重构之名顺便修改，除非它会阻止拆分或编译。
- 不允许一次性大规模重写。

## 一、重构目标

目前部分代码过度集中，需要根据职责拆分为清晰、可维护、可测试的模块和组件，同时严格保持原来的：

- 功能行为
- 页面交互
- API 参数及返回结构
- Tauri command 名称
- 前后端事件名称
- 状态流转
- 扫描流程
- SSH 执行逻辑
- 错误处理语义
- 数据持久化格式
- 用户设置兼容性

重构前后，相同输入必须产生相同输出和副作用。

## 二、第一步：只分析，不修改

先扫描当前项目并输出一份《模块化重构分析》，至少包括：

1. 前端目录结构和主要入口。
2. Rust/Tauri 后端目录结构和主要入口。
3. 超大文件、超大组件和职责混杂模块。
4. 每个目标文件承担了哪些职责。
5. 模块之间的调用关系、状态依赖和事件依赖。
6. 哪些公共类型、常量、工具函数存在重复。
7. 哪些部分风险较高，不适合当前阶段拆分。
8. 推荐的拆分顺序。
9. 每个阶段涉及的文件。
10. 每个阶段的验证方法。

请用以下等级标注风险：

- 低风险：纯函数、展示组件、类型、常量、格式化工具。
- 中风险：Store、Hooks、API 封装、事件监听和状态同步。
- 高风险：SSH 会话、扫描任务、并发控制、取消机制、Tauri 状态、远程命令、安全策略。

分析完成后，再按照阶段逐步实施。

## 三、拆分原则

### 1. 前端

按照真实代码职责选择合适结构，不要为了目录好看而过度拆分。可以参考：

```text
src/
  features/
    project-discovery/
      components/
      hooks/
      services/
      stores/
      types/
      utils/
      index.ts
    file-manager/
    monitoring/
    server-connection/
  shared/
    components/
    hooks/
    services/
    types/
    utils/
````

建议拆分方向：

* 页面组件只负责页面布局和功能组合。
* 展示组件通过 props 接收数据，不直接调用 Tauri。
* Tauri invoke 和事件监听放入独立 service/API 模块。
* 复杂状态流程放入 hook 或 store。
* 类型定义独立存放。
* 格式化、路径处理、状态判断等纯函数放入 utils。
* 弹窗、列表项、状态条、工具栏等可独立为组件。
* 不要把所有逻辑从一个大文件转移到另一个“大 hook”中。
* 不要为了拆分制造大量只有几行且没有独立职责的文件。

### 2. Rust/Tauri 后端

根据当前实际代码进行拆分，可以参考：

```text
src-tauri/src/
  commands/
  services/
  domain/
  adapters/
  infrastructure/
  state/
  events/
  security/
```

建议职责：

* `commands`：参数接收、权限检查、调用 service、转换返回值。
* `services`：业务流程编排。
* `domain`：核心数据结构、状态、规则和纯逻辑。
* `adapters`：Docker、systemd、Nginx、PM2 等能力适配。
* `infrastructure`：SSH、SFTP、远程命令和系统接口。
* `state`：Tauri Managed State、任务注册表和会话状态。
* `events`：事件名称、事件结构和发送逻辑。
* `security`：命令白名单、参数校验、敏感信息处理。

必须保证：

* 已注册的 Tauri command 名称不变。
* 前端调用参数字段不变。
* serde 序列化字段不变。
* 事件名称及 payload 不变。
* 状态生命周期不变。
* 并发、超时、取消和清理逻辑不变。
* 远程命令内容和执行顺序原则上不变。

如果移动公共结构体，优先通过 `pub use` 保持已有引用兼容，减少一次性修改范围。

## 四、实施顺序

必须分阶段实施，每个阶段完成后立即验证。

### 阶段 A：建立行为基线

在修改前执行并记录当前可运行的检查，例如：

```bash
npm run typecheck
npm run lint
npm run build
cargo fmt --check
cargo check
cargo test
```

根据项目实际使用的包管理器和脚本调整命令，不要盲目执行不存在的脚本。

如果原项目本身已有失败项：

* 明确记录失败命令。
* 保存关键错误信息。
* 不要把原有失败误判为本次重构引入。
* 重构后不得增加新的失败。

为高风险逻辑补充“行为保护测试”，但测试应验证当前行为，不得重新定义业务规则。

### 阶段 B：低风险拆分

优先处理：

* TypeScript/Rust 类型
* 常量
* 状态枚举
* 事件名称
* 格式化工具
* 路径工具
* 纯函数
* 无状态展示组件

完成后运行相关检查。

### 阶段 C：前端组件拆分

逐个拆分大页面：

* 页面容器
* 工具栏
* 列表
* 列表项
* 状态显示
* 空状态
* 错误状态
* 对话框
* 详情面板

保持原有 DOM 行为、交互入口、按钮状态和调用时机。

一次只拆一个明确职责，不要同时重构多个业务模块。

### 阶段 D：前端逻辑拆分

按职责抽取：

* Tauri API service
* 事件订阅 hook
* 扫描任务 hook
* 轮询逻辑
* Store selectors/actions
* 状态转换纯函数

重点检查：

* `useEffect` 清理函数是否改变执行时机。
* 依赖数组变化是否造成重复订阅或提前取消。
* 组件卸载时才执行的逻辑不能在普通状态更新时触发。
* 定时器和监听器是否完整清理。
* 异步请求是否产生竞态。
* 任务开始、进度、完成、失败、取消状态是否保持一致。

### 阶段 E：Rust 后端拆分

先移动纯类型和纯函数，再拆 service，最后才处理：

* SSH 会话
* 后台任务
* 任务注册表
* 并发控制
* 取消令牌
* Tauri Managed State
* 事件发送
* 远程命令执行

高风险模块尽量只做代码移动和依赖调整，不同时改算法。

### 阶段 F：清理和文档

确认行为一致后再进行：

* 删除确认无引用的旧代码。
* 统一模块导出入口。
* 修复重复导入。
* 补充模块说明。
* 更新与实际代码不一致的架构文档。

不得删除“看起来没有使用”但可能通过 Tauri、事件、序列化或配置动态引用的代码，除非已经搜索并验证。

## 五、禁止事项

本次重构禁止：

* 更改产品功能。
* 改变扫描算法或项目评分规则。
* 改变远程命令。
* 改变接口字段。
* 改变数据库或配置格式。
* 改变路由和页面交互。
* 更换状态管理框架。
* 引入重量级新依赖。
* 大规模重新命名公开接口。
* 同时修复大量历史问题。
* 删除暂时无法理解的兼容逻辑。
* 把多个独立职责重新集中进一个新文件。
* 在没有测试或验证的情况下声称“逻辑完全不变”。

如果必须改变逻辑才能完成拆分，立即停止这一部分，并说明：

1. 为什么必须改变。
2. 会影响哪些行为。
3. 是否存在保持兼容的方案。
4. 建议单独建立什么修复任务。

## 六、每个阶段的输出格式

每完成一个阶段，请输出：

```md
## 本阶段完成内容

- 拆分了什么
- 新增了哪些模块
- 旧文件保留了什么职责

## 文件变更

| 文件 | 操作 | 职责 |
|---|---|---|
| path | 新增/修改/移动 | 说明 |

## 行为兼容性

- 保持不变的接口
- 保持不变的状态流程
- 保持不变的事件和副作用
- 可能存在的风险

## 验证结果

- TypeScript：
- ESLint：
- 前端构建：
- cargo fmt：
- cargo check：
- cargo test：
- 其他测试：

## 下一阶段

- 下一步准备拆分的内容
- 风险等级
```

## 七、最终验收标准

只有同时满足以下条件，才可以认为模块化重构完成：

* 最新代码已被完整检查。
* 主要大文件已经按职责合理拆分。
* 页面组件不再承担全部业务逻辑。
* Tauri command 主要负责边界层工作。
* 公共类型、事件、API 和工具有明确归属。
* 没有形成新的超大组件或超大 hook。
* 用户可见行为没有变化。
* API 和事件协议没有变化。
* 原有功能仍可正常运行。
* 没有新增编译、测试或格式化失败。
* 高风险模块有明确验证记录。
* 文档与最新目录结构一致。

现在先执行“第一步：只分析，不修改”，输出模块化重构分析和分阶段计划。不要立即进行全量代码修改。

````

建议第一轮只让 Cursor 输出分析报告。你确认拆分边界后，再给它这条追加指令：

```md
分析方案已确认。现在只实施阶段 B（低风险拆分）。

要求：

- 不得提前实施后续阶段。
- 一次处理一个职责。
- 每次移动代码后立即运行最小相关检查。
- 保持所有公开接口、Tauri command、事件名称和 serde 字段不变。
- 如果当前工作区代码与之前的分析不一致，以当前最新代码为准并调整方案。
- 完成后按约定格式报告变更和验证结果，等待我确认后再进入阶段 C。
````

核心原则就是：先建立行为基线，再拆纯代码，然后拆 UI，最后才碰 SSH、扫描、并发和任务生命周期。这样最不容易在“模块化”过程中悄悄改变原来的逻辑。

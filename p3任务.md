请先完整检查当前仓库的最新代码、README、已有阶段文档、Rust/Tauri 命令层、React 状态管理、SSH/SFTP 会话复用方式以及现有测试，再开始修改。

执行要求：

以仓库现有代码为准，不凭空假设文件路径、组件名称或数据结构。

复用 P1 的 SSH 会话和 P2 的服务器连接状态，不建立相互独立的重复连接系统。

P3 是只读的“项目发现与部署评估”阶段，不执行安装、构建、重启、部署或配置覆盖。

不允许使用演示数据、随机数据、写死的项目列表或伪造扫描结果。

不允许由前端拼接任意 Shell 命令；扫描命令、参数、白名单与超时必须由 Rust 后端控制。

每完成一个子任务都要补充或更新测试；最终运行仓库已有的 lint、typecheck、test、Rust test 和 build 检查。

如果现有架构与本文档冲突，优先保持项目架构一致，并在交付说明中列明调整原因。

不要顺手重构与 P3 无关的模块，不删除已有功能，不覆盖用户未提交的修改。

二、P3 产品目标

P3 需要解决以下问题：

用户不知道项目部署在哪个目录。

同一服务器可能同时存在 Java、Node.js、Python、Go、Rust、PHP、.NET 和容器项目。

一个仓库可能包含多个模块或二十多个微服务，不能全部误判成互不相关的项目。

服务器可能存在源码、旧版本、备份、构建产物、依赖目录和运行中的生产项目，系统必须区分。

每个候选项目必须有可追溯的证据和权重，不能只因为目录中出现一个文件就列为项目。

扫描结果需要成为后续 AI 生成部署方案、分析依赖和诊断故障的可靠数据基础。

目标操作流程：

用户连接服务器。

点击“发现服务器项目”。

系统进行受控的全服务器候选扫描。

系统按证据计算项目置信度。

只对达到候选阈值的目录进行深度识别。

系统合并仓库、模块和微服务关系。

页面显示项目路径、类型、运行状态、判定依据和部署准备度。

用户确认、忽略、合并或拆分项目。

确认结果交给后续 P4 生成部署方案。

三、扫描原则：全服务器，但不是无差别读取

“全服务器扫描”是指系统主动覆盖服务器上的主要应用位置，并结合运行环境反向定位项目；不是从 / 开始无限递归读取所有文件内容。

采用两阶段扫描：

阶段 A：广度候选发现

只扫描目录元数据、有限层级和项目标志文件，不读取全部源码。

默认扫描根目录：

/home

/root（当前账号有权限时）

/srv

/opt

/var/www

/data

/app

/apps

/workspace

/usr/local 下的应用目录

同时通过以下运行证据反向发现路径：

Git 工作区根目录

systemd 单元的 WorkingDirectory、ExecStart、EnvironmentFile

运行进程的工作目录、命令行和可执行文件路径

Docker 容器的 bind mount、工作目录、镜像标签和 Compose 标签

Docker Compose 文件及其 service、build context、volume

Nginx 的 root、alias、proxy_pass 和 upstream

常见项目清单、工作区和构建文件

阶段 B：候选深度识别

仅对达到候选分数阈值的目录执行：

技术栈识别

模块和工作区解析

启动方式识别

运行服务关联

端口关联

环境变量名称识别

依赖服务识别

部署文件和部署阻塞项分析

禁止在 P3 中：

全量下载源码到本地

读取 .env、私钥、Token、密码等秘密值

把服务器文件全文发送给前端或 AI

修改远程文件

安装软件或执行部署

四、默认排除与扫描边界

默认排除系统及高噪声路径：

/proc

/sys

/dev

/run

/boot

/lib、/lib64

/usr/bin、/usr/lib、/usr/share

/var/lib/docker

/var/cache

/var/log

临时目录和回收目录

项目内部默认跳过：

.git/objects

node_modules

vendor

.venv、venv

target

dist

build

.next

.cache

coverage 和日志目录

扫描安全边界：

默认不跨文件系统挂载点；额外挂载点由用户主动开启。

不跟随符号链接越界，必须防止符号链接循环。

默认最大扫描深度建议为 6，做成后端配置常量。

扫描任务必须支持超时、取消、进度事件和错误汇总。

权限不足只产生警告，不得让整个扫描失败。

对目录数、候选数和并发数设置上限，避免服务器负载异常。

首次扫描完成后，根据路径、mtime 等信息支持增量重扫。

五、项目证据与权重模型

不得只返回一个最终分数。每个加分、扣分和来源都必须结构化保存，并能在 UI 中解释。

5.1 正向证据

分类

示例

分类上限

项目标识

.git、package.json、pom.xml、build.gradle、Cargo.toml、go.mod、pyproject.toml、composer.json、.sln、.csproj

30

源码结构

src/、app/、源码扩展名、入口文件、模块目录

15

部署能力

Dockerfile、Compose、启动脚本、systemd 配置、Nginx 配置、部署文档

20

运行证据

活跃进程、systemd、Docker、Nginx 正在引用该目录

25

项目完整性

lockfile、README、CI 配置、环境变量模板、测试目录

10

建议基础权重：

存在有效构建清单或包清单：+20

存在 Git 工作区：+10

存在合理源码结构：+5～15

存在 Dockerfile 或 Compose：+10

存在明确启动/服务配置：+5

活跃进程的工作目录或执行路径属于候选目录：+15

systemd 单元明确指向候选目录：+10

Docker 或 Nginx 明确关联候选目录：+5～10

存在 lockfile、环境模板、README、CI：按项加分，但总分不超过分类上限。

所有分类封顶，最终总分限定在 0～100。

5.2 负向证据

只有构建产物、没有项目清单和源码结构：-30

只有依赖、缓存或生成目录：-40

明确属于系统软件目录：直接排除

备份目录、归档目录或带明显时间戳的旧副本：降低置信度并标记“可能是备份”

子目录证据完全被父级工作区覆盖：不作为独立顶级项目，合并为模块

只有单个弱标志文件且没有其他证据：不得进入高置信度列表

5.3 置信度分级

75～100：高置信度项目，默认展开显示

55～74：很可能是项目，需要用户确认

35～54：可能是项目，折叠显示

< 35：不进入项目列表，但可保留在诊断统计中

分数相同时，优先排序：

有运行证据的项目

有部署配置的项目

最近有变化的项目

路径层级更合理的项目根目录

六、项目类型与模块关系

至少识别：

Node.js / React / Vue / Next.js / NestJS

Java Maven / Gradle / Spring Boot

Python pip / Poetry / uv / Django / Flask / FastAPI

Go

Rust / Cargo

PHP / Composer

.NET

Docker / Docker Compose

静态站点

未知类型，但证据充分的项目

工作区和模块解析至少覆盖：

Maven <modules>

Gradle settings.gradle / settings.gradle.kts

npm、pnpm、Yarn workspace

Cargo workspace

Go workspace

.NET solution/project

Docker Compose services

同一 Git 根目录下的模块

合并规则：

一个仓库包含多个模块时，默认展示为一个顶级项目和模块树。

模块如果有独立 Git 根目录、独立运行配置或明确独立部署证据，可标记为独立可部署单元。

同一路径被 systemd、Docker、Nginx 多次引用时合并证据，不能生成重复候选。

用户可手动“合并为一个项目”或“拆分为独立项目”，决定需要本地持久化。

七、建议数据模型

请结合现有 Rust 和 TypeScript 类型系统实现，字段名称可以按项目规范调整，但语义不得丢失。

type ConfidenceLevel = "high" | "likely" | "possible";

interface ProjectEvidence {
  id: string;
  kind: string;
  source: string;
  summary: string;
  weight: number;
  verifiedAt: string;
  sensitive: boolean;
}

interface ProjectPenalty {
  kind: string;
  summary: string;
  weight: number;
}

interface RuntimeLink {
  kind: "process" | "systemd" | "docker" | "nginx";
  name: string;
  status?: string;
  ports?: number[];
  source: string;
}

interface ProjectModule {
  id: string;
  name: string;
  path: string;
  projectType: string;
  deployable: boolean;
  children: ProjectModule[];
}

interface ProjectCandidate {
  id: string;
  serverId: string;
  name: string;
  path: string;
  projectType: string;
  score: number;
  confidence: ConfidenceLevel;
  evidence: ProjectEvidence[];
  penalties: ProjectPenalty[];
  runtimeLinks: RuntimeLink[];
  modules: ProjectModule[];
  detectedPorts: number[];
  requiredEnvironmentNames: string[];
  blockers: string[];
  warnings: string[];
  updatedAt: string;
}

interface DeploymentReadiness {
  score: number;
  blockers: string[];
  warnings: string[];
  confirmedFacts: string[];
  unknownFacts: string[];
}

Rust 后端应有等价的可序列化类型，避免前后端使用无约束 JSON。

本地持久化至少包含：

最近一次扫描摘要

已确认项目

已忽略目录

用户手动合并/拆分关系

候选证据和置信度

扫描时间及缓存信息

不得保存服务器秘密值。

八、后端任务拆分

P3.1 扫描任务管理

创建、取消和查询扫描任务。

同一服务器避免重复并发全量扫描。

通过 Tauri event/channel 推送扫描阶段、进度、当前路径和警告。

SSH 断开时任务应安全结束，并返回明确状态。

重新连接后允许重新扫描或增量扫描。

P3.2 候选收集器

将候选来源拆成独立 collector，建议包括：

文件系统标志收集器

Git 收集器

systemd 收集器

进程收集器

Docker/Compose 收集器

Nginx 收集器

某个 collector 不可用时不得导致整个扫描失败。例如服务器没有 Docker，只记录“Docker 不可用”。

P3.3 证据归一化与评分器

相同证据去重。

分类封顶。

正向与负向证据统一计算。

评分逻辑集中管理并支持单元测试，不得散落在 UI 或多个命令中。

分数结果必须可解释和可复现。

P3.4 深度识别器

只分析达到阈值的候选。

解析技术栈、模块、启动配置和环境变量名称。

文件内容读取需要大小上限和允许文件类型白名单。

.env 只可发现文件存在以及必要时提取变量名；禁止读取、记录或返回值。

P3.5 部署准备评估器

只输出评估，不执行修复：

是否有构建清单

是否有锁文件

是否能识别启动入口

是否能识别运行时版本要求

是否存在部署配置

是否有必要的环境变量名称

是否有关联数据库、Redis、ES、消息队列等依赖迹象

是否存在端口冲突迹象

哪些事实仍需用户确认

九、前端页面与交互

连接服务器后，在右侧主要工作区增加“项目”入口。不要破坏现有文件浏览、终端和监控功能。

9.1 初始状态

标题：服务器项目

主按钮：发现服务器项目

说明：自动扫描常见部署位置和运行服务，不会修改服务器文件

支持“高级设置”，可补充扫描目录或启用其他挂载点

9.2 扫描中

展示：

当前阶段：候选发现 / 运行服务关联 / 深度识别 / 生成报告

总体进度

已检查目录数

已发现候选数

当前处理路径（过长时省略）

权限警告数

取消扫描按钮

不要用无限假进度；无法计算总量时使用阶段进度和实时计数。

9.3 扫描结果

提供筛选：

高置信度

待确认

可能项目

正在运行

未运行

技术栈

项目卡片至少展示：

项目名称

远程绝对路径

技术栈

置信度和分数

是否正在运行

模块/服务数量

已识别端口

部署准备度

阻塞项数量

详情区域至少包含：

“为什么判定为项目”证据列表

扣分和风险

项目文件与模块树

运行服务关联

环境变量名称，不显示值

部署准备检查结果

确认项目、忽略目录、合并、拆分操作

9.4 面向新手的阶段向导

页面顶部显示：

发现项目

确认项目与模块

检查部署环境

生成部署方案（P4，当前只显示后续阶段，不实现执行）

在关键事实不完整前，不显示可直接执行部署的按钮。

十、为后续 AI 保留的能力

P3 的确定性扫描器和证据模型是事实来源。AI 不能替代扫描器判断文件是否存在、服务是否运行或端口是否开放。

P3 需要为后续 AI 提供干净的结构化输入：

项目和模块关系

证据及来源

技术栈和版本线索

运行服务及端口

部署配置线索

环境变量名称

外部依赖线索

阻塞项、警告和未知事实

后续 AI 可以：

用新手能理解的语言解释扫描结果

只询问缺失的关键信息

生成项目部署方案和服务启动顺序

建议 Docker、Nginx、systemd 配置

生成部署前检查清单

分析失败日志并给出修复和回滚建议

AI 安全边界：

不把秘密值发送给模型。

不默认发送完整源码。

不允许 AI 静默执行命令。

任何命令和文件修改必须先展示计划、差异、影响范围和回滚方式，再由用户确认。

十一、测试要求

11.1 评分器单元测试

至少覆盖：

有 Git、构建清单、源码和运行证据的项目进入高置信度。

只有 node_modules 的目录被排除。

只有 dist 或 JAR 文件的目录不会被误判为完整源码项目。

systemd 指向的项目获得运行证据加分。

分类权重正确封顶，总分保持在 0～100。

相同证据不会重复加分。

负向证据正确扣分。

11.2 项目结构测试

Maven 多模块项目正确形成项目树。

pnpm/Yarn workspace 正确合并。

Docker Compose services 正确关联。

嵌套 Git 仓库按规则拆分。

22 个 Java 微服务可以展示为一个仓库下的多个可部署单元，而不是无依据的 22 个顶级项目。

11.3 扫描可靠性测试

权限不足目录不会中断任务。

符号链接循环不会卡死。

超大目录受到深度、数量和超时限制。

没有 Docker、Nginx 或 systemd 时可以降级完成。

扫描可以取消。

SSH 意外断开后状态正确。

ProxyJump/跳板机连接下扫描目标是最终服务器。

增量重扫不会重复生成项目。

11.4 安全测试

返回数据不包含 .env 值、密码、私钥或 Token。

前端不能传入任意 Shell 片段。

路径和参数经过严格校验。

日志中秘密信息被脱敏。

P3 的所有远程操作都是只读操作。

11.5 前端测试

空状态、扫描中、完成、部分失败、取消和断线状态正确。

进度来自真实事件。

置信度、证据、扣分和部署阻塞项可以展开查看。

筛选、确认、忽略、合并和拆分可用并能持久化。

刷新页面或重新打开应用后能恢复最近一次结果。

十二、P3 验收标准

以下项目全部满足，P3 才能通过：

用户不知道项目目录，也能通过一次操作发现真实项目。

扫描覆盖常见应用目录，并结合进程、systemd、Docker 和 Nginx 反向发现项目。

每个候选项目都有分数、证据、扣分和来源说明。

node_modules、缓存、构建目录和系统目录不会被当成顶级项目。

正在运行的项目可以关联进程、服务、容器或代理证据。

多模块仓库和微服务可以形成正确的项目树。

页面可查看路径、技术栈、模块、端口、运行状态和部署准备度。

扫描支持进度、取消、超时、错误汇总和增量重扫。

权限不足、命令不存在和 SSH 断开不会导致应用崩溃。

所有扫描操作只读。

服务器秘密值不进入前端、日志、本地数据库或 AI 数据结构。

不存在演示数据、伪造项目或随机监控数据。

前端 lint/typecheck/test 通过。

Rust format/clippy/test 通过。

Tauri 开发构建及仓库现有 CI 通过。

十三、交付格式

Cursor 完成后必须输出：

本次实现摘要。

修改文件列表及每个文件的作用。

项目扫描流程说明。

实际采用的评分表和阈值。

安全边界及秘密信息处理方式。

新增测试及测试结果。

执行过的验证命令和结果。

未完成项、已知限制和进入 P4 前的风险。

不得仅回复“已完成”，也不得跳过真实测试结果。

十四、阶段边界

本阶段 P3 必须完成

全服务器受控候选发现

确定性证据采集

项目评分与解释

技术栈与模块识别

运行环境关联

部署准备评估

新手项目发现页面

结构化 AI 输入基础

本阶段明确不做

自动安装运行环境

自动构建项目

自动修改 Nginx、systemd 或 Docker 配置

自动启动、停止或重启服务

自动部署和回滚

AI 自主执行远程命令

这些能力进入 P4/P5：

P4：AI 辅助部署方案——基于 P3 证据生成环境准备、构建、部署、依赖顺序和回滚计划。

P5：安全部署执行——审批后执行命令，提供实时日志、健康检查、版本记录和失败回滚。

P6：AI 运维诊断——分析故障、资源异常和日志，输出可确认的修复建议。

十五、最终实现原则

P3 的核心不是“扫描出了多少目录”，而是：

系统能否通过真实、确定、可解释的证据，帮助一个不熟悉 Linux 的新手找到服务器上的项目，并为下一步安全部署准备完整事实。

如果识别结果无法说明“为什么这是项目”，或者仍要求用户先知道项目路径，则 P3 不算完成。
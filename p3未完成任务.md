修复 BLS-OPS“应用服务/基础设施”分类和重复展示问题。

核心原则：

1. 顶层 Tab 必须互斥。
2. “运行方式”“技术类型”“业务角色”“项目关系”必须拆开。
3. MySQL/Redis 虽然正在运行，但顶层只进入基础设施。
4. Node、Java、Python、Go、自定义业务容器进入应用服务。
5. 未识别服务不能默认归基础设施。
6. 静态前端项目不伪造运行进程。
7. Nginx 站点是项目访问入口，不是独立运行实例。
8. 项目详情可以关联展示运行服务和基础设施，顶层列表不能重复。

一、扩展 DeploymentInstance

新增：

- workload_role:
  application | infrastructure | system | unknown
- component_role:
  frontend | backend | worker | database | cache | gateway |
  message_queue | search | storage | observability | unknown
- technology
- linked_project_ids
- ownership:
  shared | project_scoped | unknown
- classification_evidence

不要让前端通过 service?.group 临时推断最终角色。

二、修改分类逻辑

分类优先使用：

1. system_owned
2. 明确基础设施身份
3. 已确认项目关联
4. 源码构建清单
5. Compose labels / working_dir / bind mount
6. 启动命令与进程 cwd
7. 镜像名
8. 端口只能作为辅助证据

规则：

- MySQL、Redis、PostgreSQL、MongoDB：
  workload_role=infrastructure
- Node/Java/Python/Go 进程关联源码项目：
  workload_role=application
- 未识别但 source_known=true：
  优先 application 或 unknown，不得默认 infrastructure
- 未识别且 source_known=false：
  workload_role=unknown
- 共享 Nginx：
  workload_role=infrastructure
- 项目专用 Nginx 前端容器：
  workload_role=application
  component_role=frontend
  technology=nginx

三、修改前端 Tab

将“运行服务”改为“应用服务”。

应用服务：

instances.filter(i => i.workload_role === "application")

基础设施：

instances.filter(i => i.workload_role === "infrastructure")

未分类实例放在“应用服务”的“待归类”筛选中，不能塞进基础设施。

增加开发期断言：

同一个 instance.id 不允许同时出现在 applicationInstances 和
infrastructureInstances。

四、拆分 Nginx 模型

不要为每个 server block 创建一个普通 DeploymentInstance。

新增 GatewayRoute：

- id
- gateway_instance_id
- server_names
- listen_ports
- root
- proxy_targets
- config_file
- linked_project_id

Nginx daemon/container 是实例；
server block 是路由；
项目详情展示路由关系。

五、测试

必须覆盖：

1. MySQL 只出现在基础设施。
2. Redis 只出现在基础设施。
3. Node API 只出现在应用服务。
4. 未识别自定义容器不进入基础设施。
5. React/Vue 静态项目不生成虚假 Node 服务。
6. Nginx 静态站点关联前端项目。
7. Nginx proxy_pass 关联后端应用服务。
8. 项目专用 Nginx 前端容器归应用服务。
9. 同一个 instance.id 不能出现在两个顶层 Tab。
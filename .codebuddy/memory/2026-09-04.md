# 2026-09-04

## i18n 抽取（files 模块，i18n-files 代理）
- 范围：`src/workbench/views/remote-file/**`、`preview/**`、`views/test/**` 断言；语言包只写 `src/i18n/locales/zh-CN/files.ts`（约 84 条）。19 个文件，tsc 0 错误 + vitest 26/26 全绿。
- 模式沉淀：NameDialog 等"结构字段存英文 key + 渲染处 t()"；右键菜单 label 在 onContextMenu 事件回调内即时 t()（无需动 ContextMenu 组件）；PanelButton 这类小组件在内部 t(label)。
- 插值策略：已 toLocaleString/formatCount 的值走 `{{name}}` 插值保千分位；裸计数走 `{{count}}`。文件大小/时间是数据不抽。
- 纯 TS 模块（utils.ts/dirSizeSummary/validateName/PdfPreview.toMessage/DocPreview.firstLine）用 `import { i18n } from "@/i18n"` + `i18n.t()`。
- i18next v26 陷阱（i18n-libs 修复）：`parseMissingKeyHandler: (key) => key` 会把已插值结果覆盖回 `{{name}}` 占位符字面量 —— en 默认语言下插值 key 全坏。已移除该配置，勿加回。
- 测试断言中文 → 英文 key 时必须与组件实际 key 对齐（并行改造后以渲染输出为准：LogCenterView 用 "Copy this line"/'Show "{{level}}" and above'；WorkbenchHome 用 'Deleting "{{name}}" will also delete…'）。
- 跨模块联动：file-kind.ts `kind.label` 与 preview/hex.ts `MAGIC label` 改英文 key 后，渲染点（EntryIcon tooltip、FilePreviewModal 副标题、HexPreview "Detected as"）需包 t()。

## i18n 抽取（terminal 模块，i18n-terminal 代理）
- 范围：`src/workbench/views/terminal/**`（14 个源文件 + 5 个测试文件）；语言包 `src/i18n/locales/zh-CN/terminal.ts`（约 80 条，通用词复用 common：Search/Copy/Close/Retry/Copied/Enabled/Running）。terminal 范围 tsc 0 错误 + vitest 277/277 全绿；63 个 t() key 全覆盖。
- **xterm 边界**：`terminal.write/writeln` 的字符串（"已连接 host"、`命令无效：`、`连接失败：`）是终端画面不是 React UI，全部保持原样；结果快照中文（远程输出）不动；知识库 title/detail（Rust catalog）与 dockerReason/容器 state 不翻译。
- **补全链路 i18n 模式**（可复用）：无插值的 notice/title/detail 在 provider 存英文 key，渲染层统一 `t(item.detail)`/`t(notice)` —— 未命中 key 原样返回，天然兼容远程数据（路径/容器名/`command · CPU x%`）与英文专有名词（字体名/Volume）；含插值的句子（读取失败/describeEnvironment 环境摘要）在 provider 生成点直接 `i18n.t("... {{x}}", {x})`。`Failed to read Docker {{kind}} list` 用嵌套 `i18n.t(labelOf(kind))` 翻资源词。
- ParamPicker：消除 `kind === "unit" ? "服务" : …` 条件拼接 → `PARAM_LOADING_HINTS` 三条完整 key。TerminalSuggest 快捷键说明拆成两条完整 key（避免文案拼接）。
- UNKNOWN_RISK/COMMAND_SOURCE_LABELS/TERMINAL_FONTS label 常量改存英文 key；RISK_META（ops-api，i18n-libs 管辖）label 作 `{{risk}}` 数据插值，前端不 t()。
- 测试：渲染类测试（TerminalSnapshotView/terminal-selection-menu）与 provider 测试（registry/remote-directory/environment）顶部加 `import "@/i18n"`（en；未初始化时 i18next.t 返回 undefined 会让插值断言挂）。CopyNotice 断言用 "Copied"/"Copy failed. Please check clipboard permission"。
- 校验工具法：PowerShell 处理 unicode/转义不可靠 → 写临时 .mjs 脚本（正则 `(?<![a-zA-Z0-9_$.])t\("...")` 比对语言包源文本，跑完删除。


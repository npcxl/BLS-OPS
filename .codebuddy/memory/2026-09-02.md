# 2026-09-02

## 模块化重构（依据用户提供的《代码重构任务.md》，已完成 B→F 全部阶段）
- 用户不在场授权自主推进，铁律=行为等价移动、每阶段验证。分析报告与实施结果落盘 `docs/模块化重构分析.md`。
- **阶段 B**：`api/ops-api.ts` 类型拆至 `api/types/{servers,sessions,ssh,sftp,monitor,services,containers,gateway,project}.ts`（ops-api.ts re-export 全兼容，ops-api.test.ts 零改动通过）；`lib/format.ts`（formatBytes/formatSpeed/formatUptime/formatSize/formatTime/formatCount，两套字节梯度按原语义共存）；`lib/events.ts`（sshOutputEvent/sshClosedEvent/servicesChangedEvent/projectScanResultEvent + re-export DIRECTORY_SIZE_EVENT）。
- **阶段 C**：四大视图目录化，旧路径留 re-export stub（TabContent 改 lazy 指向新路径）：`views/terminal/`、`views/server-monitor/`、`views/project/`、`views/remote-file/`。引用面只有 TabContent + TerminalView→RemoteFilePanel。
- **阶段 D**：`hooks/use-scan-task.ts`（ProjectView 轮询+事件+取消+清理整套搬出）；`views/remote-file/use-dir-size-queue.ts`（目录大小队列 ≤2 并发 + useAutoDismiss/useTransientNotice）。
- **阶段 E**：`commands.rs`（2604 行）→ 父模块（open_db/record_audit/require_existing_credential/test_support::db + `pub use` re-export）+ `commands/{servers,credentials,known_hosts,sessions,ssh,sftp,monitor,app,services,containers,gateway,project,deployment,tests}.rs`。**lib.rs 零修改**（`commands::xxx` 路径靠 glob re-export 保持）。校验函数标 pub(crate) 供 commands/tests.rs 跨子模块引用。
- 坑：子模块用 `app.emit` 必须各自 `use tauri::Emitter`（trait 方法不随 AppHandle 类型走）；私有 `mod commands` 里未注册的 pub fn（deployment 系列）报 dead_code —— 既有状态（lib.rs 有意不在 P3 暴露 deployment IPC），不阻塞。
- 最终验证：pnpm build ✓ / pnpm test 52 ✓ / cargo fmt --check ✓ / cargo test 236（167+17+25+27）✓。
- 通用经验：大文件纯移动拆分时 `delete_file` 需审批（用户不在场），用「旧路径 re-export stub」替代最稳；外发事件/常量/类型先搜全引用面再决定 re-export 集合。


## P3 重构：部署实例优先发现（用户给完整方案后实施）
- 用户确认 P3 项目发现的主流程改为「部署实例优先」：先能力识别 → 按能力启用收集器枚举**真实部署实例**（docker ps + 逐容器 inspect 提取 Compose labels/Mounts/端口；systemctl list-units + 批量 show 提取 WorkingDirectory/ExecStart；nginx -T 提取 root/proxy_pass 并用 ss→/proc/PID/cwd 反查代理后端）→ 对实例给出的候选路径**定向 marker 扫描**（第一轮），再对 /home /srv /opt /var/www /data 做 marker-only 补充扫描（第二轮）。旧的 `ProjectInventory`（find 全量枚举 2 万个文件）与 `ProjectRuntimeInventory` 已删除。
- 新模块 `src-tauri/src/deployment_collector.rs`：`DeploymentInstance { id/kind/name/status/ports/working_directories/config_files/source_paths/source_known/detail }` + 三个收集器 + 纯解析函数（parse_systemd_show/parse_nginx_effective/parse_ss_listen/docker_instance_from_inspect）带单元测试。铁律：没有宿主线索的实例 `source_known=false`（"源码未知"），绝不伪造路径；实例输出里的路径必须过 `safe::validate_abs_path`。
- safe.rs 新 Capability：`ProjectMarkerScan`（固定根第二轮）、`ProjectDirMarkers{paths}`（定向，`validate_remote_paths` 校验 ≤64 条）、`SystemdShowUnits{units}`（≤40/批）、`DockerInspectMany{containers}`（≤20/批）、`NginxEffectiveConfig`、`ListenSockets`、`ProcCwd{pid}`；共用 `PROJECT_MARKER_PREDICATE`（marker 文件 + .git/src/app 目录，不再枚举普通文件）。
- `ProjectScanResult` 增加 `instances`；`ProjectCandidate` 增加 `category`（deployed/source_only，按实例关联判定）；instance↔path 用双向前缀关联建立 RuntimeLink（取代旧的 ps/systemctl 文本匹配）。commands.rs 的 project_scan_start 重写为五阶段进度（能力→实例→定向扫描→补充扫描→评分）。
- 前端：ops-api.ts 加 DeploymentInstance/CandidateCategory；ProjectView 加「部署实例」区块（源码未知警示徽标）与 已部署/未部署源码/高置信度 筛选。
- 坑：`/etc` 在 Docker mount 源黑名单里，但 systemd FragmentPath/EnvironmentFiles 是合法配置路径 → 拆分 `is_host_project_path`（源码候选，受黑名单）与 `is_config_path`（配置文件，不受）。Rust 2021 edition 不支持 let-chains；`?` 不能用在返回 String 的闭包里（批量命令拼接改为 for 循环）。
- 测试桩欠账：p3_e2e 假服务端按 `starts_with("systemctl status -- ")` 匹配，但 ServiceStatus 命令是 `systemctl status --no-pager -- …` → 改桩为 `starts_with("systemctl status")`（HEAD 上就失败，非本次引入）。monitor_e2e 两处 DirectorySizeRegistry::start 补第 6 参 `false`（dirsize force 参数是本会话早前加的）。
- 用户在我工作期间自行提交了 `b7cf385 "Refactor project discovery and directory size computation features"`（含早前未提交改动）；后续 `git stash push/pop` 验证既有失败时未丢失改动。最终：cargo 167 单元 + 17 + 25 e2e 全绿，pnpm build 通过。

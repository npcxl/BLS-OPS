# 2026-09-08

## 文件预览 Modal 无滚动条（h-full 百分比链 → 全链 flex 传导）

- 根因：`FilePreviewModal` ready 容器（flex-col）与各 preview 根之间用
  **`h-full`（height:100%）衔接**——百分比解析依赖包含块高度 definite，
  flex 嵌套链上任何一层解析失败，该层就被内容撑开 → 超出被
  `overflow-hidden` 的 modal 裁掉 → 内容不可达且无处滚动。
- 修复（`src/workbench/views/preview/`）：**preview 根一律 `min-h-0 flex-1`
  （flex 传导），弃用 `h-full`**——TextPreview/SheetPreview/HexPreview/
  ArchivePreview/ImagePreview/PdfPreview/MediaPreview（`flex min-h-0 flex-1
  flex-col`）、DocxPreview（`min-h-0 flex-1 overflow-auto`）、SlidesPreview
  （`flex min-h-0 flex-1`）；modal ready 容器加 `overflow-hidden` 兜底。
- 与既有教训同族："浮层限高用 calc(vh)"、"高度三件套全走 style"——
  **嵌套 flex 里 height 百分比是脆弱的，全链 flex+min-h-0 才稳**。
- pnpm build ✓（preview 无独立测试文件）。

## 补全交互统一状态机大改版（用户裁决，终端 + 命令中心同一套）

- **原则：默认只有行内 ghost，面板只在 Tab / ArrowDown 后出现**。适用于全部
  补全类型（命令/Docker/Nginx/参数/cd 目录/容器名/服务名）。
- 状态机（`terminal-suggest.ts::resolveTerminalCompleteKey`，纯函数）：
  - collapsed：Tab/↓ = `accept-first`（填 hits[0]/items[0] + 展开）；Enter =
    `run-first`（直接执行第一条，参数/风险流程照走）；无候选全部穿透。
  - expanded：↑↓ 移动、Enter = `run-active`（执行当前项）、Tab/→ =
    `accept-active`（填入当前项，filled 保持展开→多级目录连续补全）。
  - 任何状态 Esc = `clear-line`（远程 Ctrl+U + 本地 LineEditor 清 + 关面板）。
  - 展开后**任何非程序性 draft 变化 → 回 collapsed**。
- 终端接线（TerminalView）：
  - `dismissedDraft` 机制**退役**（Enter 从"填入"改"执行"，无第二次 Enter 问题）；
    换成 `programmaticDraftRef`：程序性写行（applySuggestion/paramPicker/手填
    主体）标记 draft，draft effect 比对——相同=程序性，保持 expanded；
    不同=用户编辑，回 collapsed。**填入后必须先设 ref 再 setDraft**。
  - `suggestionsEnabled` 去掉 dismissedDraft 条件（空输入门控已有）。
  - 面板渲染条件 `suggestExpanded && !paramPicker`；ghost 渲染条件
    `!expanded && !paramPicker && ghostText!==""`。
  - `terminal-ghost.tsx`（新）：`pointer-events-none` 光标锚点灰色文本 + Tab
    徽标，字体/行高取 xterm options。**不是面板**——不挡点击/复制。
  - `ghostTextFor(item, line, parsedPrefix)`：token 类 insertText 减 parsedPrefix
    （`cd o`→`ps/`）；全语法类减整行（`docker p`→`s -a`）；都不匹配（裸 cd
    `" fd/"`）→ 整条显示（**ghost 自带前导空格**）；空输入/无候选 → ""。
  - ArrowUp 在 collapsed 穿透（shell 历史）。
- 命令中心（CommandCenterView）：
  - 删 "x hits" 命中数；collapsed Enter 固定执行 `hits[0]`（expanded 改字后
    activeIndex 可能残留，绝不能用它）；ArrowDown collapsed = 填入+展开；
    onChange 里 `expanded && setExpanded(false)`。
- 终端 cd 补全：
  - Provider `matches` 接管 `index===0`（裸 cd）+ `index===1`；bareCd 判据 =
    index 0 && tokens.length===1 && cursor>=token.end；**不能用 parsed.prefix
    （是 "cd"）**，按空参解析；insertText 前加空格、replaceRange 从 cursor 起。
  - `remote-cwd.ts` 加 `uncertain`：cd 提交后 exitCode===null（无 OSC 133 D
    也无 OSC 7）→ path 保留旧值但 uncertain=true；`needsProbe` 纳入 uncertain；
    cd 失败（exitCode!==0）是**确定态不标 uncertain**；OSC 7 / 成功 cd /
    probe 都清 uncertain。
- 测试：`terminal-suggest.test.ts` 重写键盘段（状态机+ghost）；provider
  remote-directory 27 条（裸 cd 5 条）；`remote-cwd.test.ts` +5（uncertain）；
  **集成测试** `terminal/test/terminal-cd-completion.integration.test.tsx`（新，
  11 条）：真实 tracker+registry+useTerminalCompletion+假 SFTP（记录
  `sessionId::path`），mock opsApi 只留 sshInput/sftpListDir；覆盖空输入不
  检索、裸 cd 前导空格、cd o ghost、失败 cd 用原 cwd、无标记→uncertain→
  探测刷新、双 Tab cwd/缓存隔离。harness 探测要 `useRef` 防重（re-render
  重复发）。**教训：TREE 必须有中间目录（/var）**。
- 验证：pnpm test 49 文件 680 条全过；pnpm build ✓。**cargo 侧用户自己在
  另一窗口管**（vscode.rs 是他窗口的文件，我顺手修了一处 E0308 的漏 &，
  以他为准）。

## "命令面板新交互没生效"排查（根因：exe 是旧 dist）

- 现象：用户反馈命令面板还是旧交互。排查：代码与 dist 都是最新的
  （dist 9/7 18:32），但用户运行的 `target/debug/ops-workbench.exe` 是
  **9/7 18:10** 构建的（早于命令面板改版与 splash 布局改动完成）——上次
  cargo build 被取消，exe 一直没重嵌新 dist。
- 修复：touch `build.rs` + `cargo build`（40.9s 成功；用户已关闭旧实例，
  否则 os error 5 锁 exe）。9/8 9:18 新 exe 链接完成，用户重启后生效。
- **教训（勿忘）**：前端改完只跑 `pnpm build` 对用户不可见——必须
  `touch src-tauri/build.rs` + `cargo build`（或 pnpm tauri dev）才有新 UI；
  用户报"没生效"先 diff dist 时间 vs exe 时间，再查进程在跑哪个 exe。
  （注：`cargo build` 的 debug exe 嵌入的是构建时的 frontendDist，
  `devUrl` 只在 `tauri dev` 时生效——直接双击 debug exe 看到的是嵌入 dist。）

## 监控域 i18n 补齐（中英适配）

- 用户报监控页中英没适配好 + "天，时"未翻。排查：`ServerMonitorView`/tables 大部分已 t()，
  缺漏在语言包侧——zh-CN/monitor.ts 缺明细 tab（Disk/Processes）、表头（Size）、
  ServiceManager 状态词（Starting/Stopping/Autostart）、LogCenter 表头与级别
  （Logs/Level/Unit/Message/Lines/Following/Current + journald 优先级 Emergency…Other）。
- `formatUptime`（lib/format.ts）硬编码"天/小时/分"→ i18n 化：key 进 **common.ts 时间分组**
  （format.ts 跨模块共享）：`{{days}}d {{hours}}h` / `{{hours}}h {{minutes}}m` / `{{minutes}}m`，
  英文紧凑显示 "3d 2h"，zh "3 天 2 小时"。函数在渲染期被调用，`i18n.t()` 取当前语言。
- zh-TW/monitor.ts 重写同步（顺手修 6 处简体残留）；重复 key 遵守"模块文件不重复通用词"
  （All Levels 在 errors.ts、Time (UTC) 在 workbench.ts，monitor.ts 不重复）。
- 验证 `pnpm build` ✓；随后 touch build.rs + cargo build 重嵌 dist。

## 远程文件夹 → VSCode（Remote-SSH）新功能

- 用户追问此前提的"右键文件夹加到本地 VSCode 且实时更新"做没做——**没做**（需求曾丢失，未进记忆/路线图）。
  教训：口头需求必须立即记入 memory，否则必丢。
- 方案确认（用户裁决 2026-09-08）：**Remote-SSH 自动注册 Host（写 ~/.ssh/config 打标记块）+ 允许私钥导出到 ~/.ssh/bls-ops/**（Rust 侧写盘、unix 0600、不经前端）。
- 实现：
  - 新域 `src-tauri/src/commands/vscode.rs`：纯函数 `sanitize_alias`/`build_host_block`/`upsert_block`/`existing_block`/`block_matches`（幂等 upsert，同 alias 三元组一致复用、不一致找 `<slug>-2…`；同前缀 alias 防混淆）；命令 `vscode_open_remote_folder(state, server_id, path)`；6 个内嵌单测。
  - 复用 `editor_sync::find_editor("vscode")` 探测；Windows `.cmd` 经 `cmd /C` + **raw_arg 显式引号**（cmd 解析器不遵守 MSVCRT 规则，& 会断行）；`Code.exe` 直接 spawn。路径校验复用 `safe::validate_abs_path`（白名单天然挡引号/空格/&）；host/user 过 `validate_host_value`（禁控制字符/引号）。
  - 标记块格式：`# bls-ops:begin <alias>` … `# bls-ops:end`，幂等原地更新，手工可清理。Host key 不写 accept-new（沿用 VSCode/OpenSSH 默认 ask = 人工确认铁律）。
  - 前端：`api/types/vscode.ts` + `opsApi.vscodeOpenRemoteFolder(serverId, path)` + RemoteFilePanel 文件夹右键 "Open in VSCode"（仅保存过服务器且非 ProxyJump；quickTarget 无稳定 alias 不显示）；失败走 `useTransientNotice`。
- **重要发现**：`editor_sync` 域（远程文件/目录副本→本地编辑器打开→保存 SFTP 回传，含 locator 探测 VSCode/Cursor/Windsurf/Trae/CodeBuddy）**后端完整但前端零调用**——之前某会话的半成品，本次未接（与 Remote-SSH 是互补的两种模式，后续可做单文件编辑入口）。
- 并行会话提醒：验证时撞见 lib.rs 中间态笔误 `journal_query,s`（对方自己已修复）；**工作区有并行会话在改 terminal ghost/cd-completion**。
- 验证：cargo fmt ✓ / cargo test --lib vscode 6✓ / pnpm build ✓ / pnpm test 680✓ / cargo build ✓（exe 已重嵌）。
- 待用户实测：Remote-SSH 首连 VSCode 会弹指纹确认（设计如此）；密码凭据服务器连接时手输密码。
- 右键菜单 VSCode 项换品牌图标：新组件 `components/ui/vscode-logo-icon.tsx`（内联 vscode-icons `file-type-vscode` body，@iconify/react 离线渲染，多色 fill 不吃 text 色类）；`ContextMenuItem.icon` 类型放宽为 `React.ElementType`（lucide 照常兼容）。

## 终端命令块悬浮复制（2026-09-08）

- 需求：终端里输入命令+返回输出，鼠标悬浮整块高亮，点击复制命令/复制输出，报错块同样可用。
- 实现（全走受控边界，不猜）：
  - 新纯逻辑 `terminal-command-blocks.ts`：块模型（start/end 两枚 xterm `IMarker` —— **存 marker 不存行号数值**，回滚 trim 时行号整体前移，marker 自动跟随；line=-1=淘汰→块隐藏）；`beginBlock`（作废遗留未完成块+追加+超限挤最老）/`finishBlock`（封最后一个未完成）/`blockAtLine`/`bufferLineAtY`/`blockRectPx`/`isFailedBlock` 纯函数。
  - 接线全在 `use-terminal-results.ts`：`execute` 注册捕获 marker 同一时刻 beginBlock；`onResult`（D 标记已写完输出、提示符未回写时刻）registerMarker 封口，exitCode/renderedText 取自 CapturedResult（复制内容与结果抽屉一致）。`blocksRef` 真源模式（同 enhancedRef）；enhanced 关/unmount 时 `clearCommandBlocks` dispose。
  - 悬浮层 `TerminalCommandBlocks.tsx`：整层 pointer-events-none 只按钮可点（交互铁律）；mousemove→测量 `.xterm-rows` 行高→viewportY+行→命中→高亮矩形+块顶按钮条（复制命令/复制输出/红字 exit 徽标）；滚轮+resize 重算；alternate screen 不渲染（TerminalView 挂载条件 `!inAlternate`）；复制走就地 useCopyFeedback+CopyNotice。
  - i18n：`Copy output` 新增 zh-CN/zh-TW；`Copy command` 复用抽屉已有 key（**语言文件里同 key 不能重复出现**，TS1117）。
- **测试抓到真 bug**：`beginBlock` 原写 `slice(length - max)`，长度<max 时是负索引截尾丢块（14 个新测试中 2 个失败暴露）——切片挤出必须先算 `overflow=length-max`，>0 才切。
- 验证：pnpm build ✓ / vitest 694 全绿（新 14）✓ / cargo build ✓ exe 已重嵌。
